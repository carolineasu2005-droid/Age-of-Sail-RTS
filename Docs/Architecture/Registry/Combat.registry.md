# Combat Gameplay Registry

**Registry status:** Foundation Phase 4B.1 Ownership Correction / Phase 4 Gate Pending Manual Validation

This registry freezes the approved ownership and dependency boundaries for the
Combat Gameplay Foundation. Phase 1A activates the authoritative per-ship
`ShipCombatState` owner and its controlled mode/target state. Phase 1B adds
independent Port/Starboard Ready-to-Reloading-to-Ready state, owner-only reload
progression, shared temporary reload configuration, and Gelderland integration.
Phase 2A added the geometric Fire Eligibility calculation and temporary
broadside-arc/range configuration. Phase 2B completes the read-only Foundation
eligibility decision by combining structural/caller-provided target legality,
the applicable authoritative broadside reload state, a narrow Combat Geometry
obstruction query, and an explicit not-yet-connected lifecycle bridge. Later
physical firing and resolution contracts remain `Planned`; architecture
approval is not implementation evidence.

Phase 2C adds an Editor-only `ShipTestPanel` consumer for explicit shooter and
target inspection. The immutable result now preserves the exact obstruction
origin/destination used by its authoritative query so debug presentation does
not reconstruct that spatial fact. The Scene visualization adds no runtime
state, query owner, or gameplay decision.

Phase 3A activates atomic Manual Target / Auto Fire arbitration on the existing
`ShipCombatState` owner. A valid Manual Target assignment disables Auto Fire;
explicitly enabling Auto Fire clears Manual Target. Assignment validates only
the minimum Combat Root identity and remains independent of current Fire
Eligibility, Movement, Blind Fire, target discovery, and firing execution.

Phase 3B activates stateless Auto Target scoring for candidates already
accepted by Fire Eligibility. The explainable score is exactly normalized
Target Exposure multiplied by a temporary linear Range Quality. Scoring reads
registered Combat Art and Movement pose contracts without discovering,
selecting, retaining, steering toward, or firing at a target.

Phase 3C activates stateless Auto Target selection over an explicit set of
immutable candidate values. Phase 3E corrects the result to preserve the
existing broadside boundary: one pass keeps at most one best legal Port target
and at most one best legal Starboard target. The caller-supplied candidate list
has no artificial gameplay count limit. No authoritative ship enumeration or
faction/allegiance Source of Truth exists, so each caller currently supplies
only a target Ship Root and its relationship-allowance fact. The selector gates
on Auto Fire with no Manual Target, evaluates authoritative Fire Eligibility
before scoring, and routes each accepted candidate only by that result's
`CombatSide`. It discovers no objects, retains no CurrentAutoTarget, steers
nothing, and fires nothing.

Phase 3D connects single-selection player right-click input to the existing
Manual Target command. A clicked Combat hierarchy child resolves upward to the
authoritative Root carrying the registered `ShipCombatState`; the controllable
shooter still comes from the existing `ShipDestinationController` selection.
Accepted assignment uses the Phase 3A atomic
command, so Auto Fire turns off without issuing any Movement command. Invalid,
non-Combat, self, or multi-selection targeting attempts do not consume the
click and preserve the existing destination/formation path. Editor-only debug
presentation now consumes explicit Phase 3C/3E candidates and results without
creating a production discovery owner.

Phase 3F completes the Editor-only verification surface for Phase 3. The Ship
Test Panel owns a dynamic add/remove/reorder list of explicit debug candidates,
passes the complete list to the existing Phase 3E selector, and presents
independent Port and Starboard result diagnostics and Scene lines. It does not
add candidate discovery, duplicate eligibility/scoring rules, persist targets
into runtime Combat state, or change any gameplay contract. Phase 3 remains
pending the developer-run Manual Unity Gate.

Phase 4A activates a targetless Blind Fire aim and eligibility query. Explicit
World-point and World-direction inputs resolve through the same pure broadside
classification used by target-aware Fire Eligibility. Point mode copies the
finite point, measures direct Root-to-point World distance, and applies the
inclusive Maximum Range; direction mode copies a normalized horizontal World
direction and deliberately has no point, distance, or range verdict. Both read
only the applicable broadside readiness and the current always-allow lifecycle
bridge. They acquire no target, query no Exposure or obstruction, commit no
reload, and write no Movement or Combat state.

Phase 4B activates the authoritative per-action Blind Fire command. Phase 4B.1
assigns that action to the dedicated stateless `ShipBlindFireCommand` service,
leaving `ShipFireEligibility` an authoritative read-only evaluator. The command
accepts an immutable Phase 4A aim, requests fresh eligibility from
`ShipFireEligibility` using current Root pose and reload state, requires exactly
one legal side, and delegates the one reload transition to
`ShipCombatState.TryCommitBroadsideFire`. Only a successful commit disables Auto
Fire; it leaves Auto Fire off and preserves any Manual Target. Rejection changes
neither broadside timer, Auto Fire, Manual Target, nor Movement. The accepted
immutable result is an aim/fire-intent snapshot only: Dispersion, Shot Samples,
Projectiles, damage, and automatic VFX emission remain `Planned`.

Phase 4C adds a Foundation-safe player-command adapter without assigning a new
keyboard binding. The existing Editor Ship Test Panel can arm Blind Fire for
one next valid right-click World/sea point. That consumed attempt delegates to
the Phase 4A query and Phase 4B action, whether accepted or rejected, and never
falls through to Manual Target, Destination, or formation placement. Arming is
transient input-layer state only, clears on selection change/cancel/attempt,
and is not Combat target ownership. Editor diagnostics display the immutable
adapter/Combat results and are not gameplay authority. Phase 4 closure remains
pending the developer-run Unity tests and Manual Gate A–L.

Combat depends on the Movement authority boundary rather than replacing it:

- `MOV-REF-001` is the authoritative Ship Root.
- `MOV-STA-001` is authoritative world position.
- `MOV-STA-002` is authoritative physical Heading / `transform.forward`.
- Combat is a read-only consumer of those contracts. The Phase 1B state owner
  has no Movement reference and performs no Movement read or write. The Phase
  2B evaluator reads Root position/orientation, authoritative Combat reload
  state, and registered Combat Art spatial inputs; it never writes pose,
  Movement state, reload state, or Combat Art data. The Phase 3B scorer reads
  only the shooter Root position as its Exposure observer and likewise writes
  no upstream state. The Phase 3C selector composes those read-only queries and
  likewise creates only a transient result; it performs no pose, Movement,
  Combat state, reload, or Combat Art write. The Phase 4A Blind Fire query reads
  only Root position/orientation and applicable broadside readiness; an illegal
  direction is rejected and never corrected by steering. The Phase 4B.1
  `ShipBlindFireCommand` repeats that query immediately before commit and never
  writes Root pose or any Movement-owned state. The Phase 4C adapter only selects this command
  route; an armed attempt never dispatches a Movement or formation command.
- Combat Art spatial inputs are defined separately in `CombatArt.registry.md`.

## Frozen dependency direction

The allowed dependency direction is one-way:

```text
Combat Art Layer (Active read-only spatial contracts)
    |
    v
Combat Foundation Layer (Active Phase 1 state/reload, Phase 2 target-aware eligibility,
                         and Phase 4A/4B targetless Blind Fire query/action,
                         with Phase 4C input adapter)
    |
    v
Combat Resolution Layer (Planned hit resolution, damage, Integrity, and lifecycle)
    |
    v
Combat Art/VFX presentation boundary (Active receiver contracts; Planned gameplay emitter)
```

Movement remains an independent authority beside the Art Layer. Combat may read
the authoritative Root pose (`MOV-REF-001`, `MOV-STA-001`, `MOV-STA-002`) and,
when an approved fire solution needs it, composed motion (`MOV-STA-011` through
`MOV-STA-015`). These reads do not grant Combat write ownership.

| Boundary input/output | Registered contract | Status at this freeze | Dependency rule |
|---|---|---|---|
| Muzzle origin and direction | `ART-SCK-001`, `ART-SCK-002` | Upstream `Active` Combat Art data | Combat copies the selected socket's World pose when creating a Shot Sample; it does not retain or mutate the socket Transform. |
| Target projected geometry | `ART-API-014`, `ART-STA-001` | Upstream `Active` Combat Art query/value | Active Auto Target scoring and future aim logic consume one stateless per-observer value; they do not query render data or mutate Exposure. |
| Hull query geometry | `ART-LYR-001`, `ART-API-008` | Upstream `Active` Combat Art query boundary | Projectile collision queries use the dedicated filter and semantic region resolution; Combat does not derive hit geometry from Render Mesh or generic `ShipCollider`. |
| Foundation eligibility obstruction anchors/filter | `ART-REF-004`, `ART-LYR-001`, `PRJ-CONV-005` | Upstream `Active` Combat Art data/convention | Phase 2B casts from shooter `CenterReference` to target `CenterReference`, against only layer 8 Combat Geometry, with `QueryTriggerInteraction.Collide`; it ignores the shooter and target hierarchies and does not treat generic `ShipCollider` or render data as Combat truth. |
| Movement pose and motion | `MOV-REF-001`, `MOV-STA-001`, `MOV-STA-002`, optionally `MOV-STA-011` through `MOV-STA-015` | Upstream `Active` Movement data | Combat reads snapshots for spatial calculations and never writes the Root, Heading, velocity, or Course. |
| Presentation output | `ART-EVT-001` through `ART-EVT-004`, `ART-API-015` | Downstream `Active` event/receiver shapes; gameplay emission remains `Planned` | Combat emits only already-resolved spatial facts. VFX cannot feed gameplay decisions or mutations back into Combat or Movement. |

## Foundation conventions

| ID | Semantic contract | Owner | Source of Truth | Writable By | Planned Consumers | Lifecycle | Status | Do Not Interpret As | Evidence |
|---|---|---|---|---|---|---|---|---|---|
| `CMB-CONV-001` | Combat must not control ship Heading. Target-aware and Blind Fire eligibility/action read the authoritative Root orientation only; an illegal Blind Fire direction is rejected rather than corrected by steering. The player adapter routes the command without issuing any Heading or navigation command. | Combat / Movement boundary | `MOV-STA-002` and this convention | Movement retains Heading writes | Known: `ShipFireEligibility`, `ShipBlindFireCommand`, and `ShipPlayerCommandInput` Blind Fire adapter. Planned: remaining Combat Gameplay and Combat AI | Foundation invariant; evaluated from current Root pose on each query/action | `Active` | Permission to steer, rotate, pursue an aim point, or issue hidden Movement commands. | Phase 2–4C rotation, precedence, Root-pose, Destination, and no-write tests. |
| `CMB-CONV-002` | Port and Starboard broadside reload are independent. A commit or timer update changes only the selected/advancing side; either or both sides may reload concurrently with different remaining times. Blind Fire execution commits exactly its freshly resolved side and leaves the opposite state/timer unchanged. | Combat Gameplay | The two separate state/timer field pairs owned by `ShipCombatState` | `CMB-API-006` may begin one selected reload; `CMB-API-013` may request exactly one such commit after revalidation; only the owner's runtime tick decrements both independent timers | Known: target-aware/Blind Fire eligibility and Blind Fire execution. Planned: Auto/Manual firing execution, UI, Basic Combat AI | Runtime per ship and per side; each completed timer independently returns its side to Ready | `Active` | Per-gun independent reload, shared ship-wide readiness/timer, half-broadside behavior, or proof that a fire action was legal without the other eligibility inputs. | `ShipCombatState.cs`; independent commit/progression/repeat-attempt tests; Phase 2B/4A eligibility tests; Phase 4B execution/isolation tests; Gelderland integration test. |
| `CMB-CONV-003` | Target Exposure, Combat Geometry, and Projectile Dispersion are independent. Exposure describes target projected geometry only; Combat Geometry supplies query-only hull geometry; Dispersion supplies Combat-owned shot error. | Combat Gameplay / Combat Art boundary | `ART-CONV-002`, `ART-API-014`, `ART-API-008`, and future Combat dispersion configuration | Separate registered owners; none mutates or becomes the Source of Truth for another | Auto Target, fire solution, Shot Sample, projectile/hit queries, validator/debug visualization | Foundation invariant | `Planned` | Exposure as accuracy randomness or collision geometry; collider/render dimensions as Exposure; Dispersion as target geometry or Hit/Miss. | Approved Phase 0.5 / Change 3 and Pre-Phase 7 architecture freeze; implementation evidence: none. |
| `CMB-CONV-004` | Exactly one participating cannon produces exactly one immutable Shot Sample after Fire Eligibility and participation are decided. | Combat Gameplay | Future authoritative participation decision plus future shot-sampling result | Future Combat fire-resolution/shot-sampling owner creates the value; consumers receive read-only copies | Projectile creation and diagnostic presentation | Per fire action and participating cannon | `Planned` | One sample per broadside, per pellet, a permission decision, a mutable projectile, or permission for half-broadside behavior. | Approved Phase 0.5 / Change 3 and Pre-Phase 7 architecture freeze; implementation evidence: none. |
| `CMB-CONV-005` | Friendly ships may block fire but do not take friendly-fire damage in Foundation. | Combat Gameplay | Future authoritative allegiance result plus Active `CMB-STA-007` obstruction | Future allegiance and Combat Resolution owners; obstruction is read-only | Fire Eligibility, Projectile/Hit resolution, Combat AI | Foundation runtime rule | `Planned` | Treating all blockers as classified friendly ships, friendly-fire damage permission, or a permanent post-Foundation policy. | Phase 2B obstruction detects filtered ship geometry without classifying friend/enemy because no allegiance Source of Truth exists; friendly discrimination and damage enforcement remain unimplemented. |
| `CMB-CONV-006` | Round Shot only in Foundation. | Combat Gameplay | Future Foundation ammunition rule | Architecture/design change only | Fire resolution, Projectile, Combat VFX, Combat AI | Foundation scope | `Planned` | Placeholder support for Chain Shot, Grape Shot, or an extensible ammunition API already being designed. | Approved Phase 0.5 / Change 3 architecture; implementation evidence: none. |
| `CMB-CONV-007` | Auto Fire uses legal targets only. Active Auto Target selection calls authoritative Fire Eligibility first, discards every result whose `CanFire` is false, and scores only the remaining legal candidates. Each accepted candidate is routed exclusively to the Port or Starboard selection reported by authoritative `FireEligibilityResult.Side`. Future fire execution must consume a still-authorized decision and is not implemented here. | Combat Gameplay | Active `CMB-STA-006`, `CMB-API-007`, and `CMB-API-009` | `ShipAutoTargetSelector` enforces the selection boundary; future Auto Fire execution owner must preserve it | Auto Target selection; planned Auto Fire execution and Combat AI | Per explicit selection evaluation; future execution must revalidate as designed | `Active` | A score as permission, scoring before legality rejection, one candidate belonging to both sides, retained permission to fire, or implemented fire execution. | `ShipAutoTargetSelector.TrySelect`; Phase 3C/3E illegal/arc/range/block/reload and per-side replacement tests; Phase 3B scorer rejection tests. |
| `CMB-CONV-008` | Manual Target and Auto Fire are mutually exclusive ship-level targeting modes. Manual Target remains exactly one ship-wide target: a successful assignment atomically stores it and disables the entire Auto Fire mode, while explicitly enabling Auto Fire atomically clears Manual Target. Clearing Manual Target does not enable Auto Fire. A successful per-action Blind Fire commit disables Auto Fire and leaves it off without clearing Manual Target; rejected Blind Fire changes neither value. | Combat Gameplay | Active `CMB-STA-003`, `CMB-STA-004`, and `CMB-API-013` on the single Combat owner | `CMB-API-001` and `CMB-API-002` perform paired mode writes; after successful reload commit `CMB-API-013` requests only Auto Fire disable through `CMB-API-001`; `CMB-API-010` is an approved caller | Known: Phase 3 arbitration/player command and Phase 4B Blind Fire action. Planned: Auto Fire execution, Combat AI/UI | Enforced on each accepted mode/action command; retained until another explicit command | `Active` | A long-lived Blind Fire targeting mode, per-side Manual Targets, mixed Manual/Auto sides, Manual aim steering, target pursuit, automatic fallback, or full Combat HUD behavior. | `ShipCombatState` commands; Phase 3 arbitration/selection/player-command tests; Phase 4B success/rejection and Manual Target preservation tests. |
| `CMB-CONV-009` | Blind Fire is a targetless per-command fire operation, not a long-lived third target-ownership mode. It resolves only an explicitly supplied World aim point or World direction, acquires and steers toward no target, and requires no target identity, relationship, Manual/Auto Target, Exposure, render data, or target-aware obstruction query. Execution re-evaluates current eligibility immediately before exactly one broadside commit. | Combat Gameplay | Explicit caller aim plus Active `CMB-API-012`, `CMB-API-013`, and adapter `CMB-API-014` | Callers/input supply aim values; `ShipFireEligibility` derives immutable results without mutation; `ShipBlindFireCommand` alone coordinates fresh evaluation and the authorized state-owner commands; Movement remains independent | Known: Blind Fire eligibility, action, and one-shot input adapter. Planned: Shot Sample handoff and Combat AI/production HUD | Per explicit query/action; no retained aim/current target/Combat mode | `Active` | Manual/Auto Target ownership, target tracking/visibility, smoke gameplay, blocker clearance, Dispersion, Shot Sample, Projectile/VFX emission, or Heading control. | Phase 4A–4C query/action/input implementations and Phase 4B.1 ownership tests. |
| `CMB-CONV-010` | Combat reads Movement Root Heading/position but does not write them. `ShipFireEligibility` uses current Root pose for target-aware and Blind Fire calculations; `ShipBlindFireCommand` requests that read-only query again for fresh Phase 4B revalidation. The Phase 4C input adapter supplies a World point and intercepts the armed click without dispatching Destination/formation or retaining pose. | Combat / Movement boundary | `MOV-REF-001`, `MOV-STA-001`, `MOV-STA-002` | `ShipSailingSpeed` retains translation writes; `ShipTurning` retains rotation writes; registered Movement commands retain Destination/formation writes | Known: eligibility/scoring/selection and Blind Fire input/action. Planned: remaining Combat spatial logic and Basic Combat AI | Foundation invariant; each query/action samples current pose without retaining it | `Active` | Transform reachability as write permission, Course as Heading, target/aim pursuit, or hidden Movement command. | Phase 2–4C implementations; Root, Heading, Destination, formation, and state-isolation tests. |
| `CMB-CONV-011` | A Shot Sample is a launch-state snapshot, not shared mutable state. It contains the source-ship and participating muzzle/side identity, the socket-derived World origin, and the sampled World launch direction or equivalent initial-velocity information sufficient for future Projectile creation. Foundation ammunition identity is Round Shot. | Combat Gameplay | Fire Eligibility/participation results, selected Active muzzle socket, future fire solution, and future Dispersion result | Future Combat shot-sampling owner at construction only | Projectile creation and diagnostics | Immutable transient value per participating cannon | `Planned` | A Transform/socket reference, required target identity, reload/readiness, eligibility, Exposure, Combat Geometry, Hit/Miss, damage, Integrity, Projectile lifetime, VFX, or UI state. | Approved Pre-Phase 7 architecture freeze; exact schema and API remain undesigned. |
| `CMB-CONV-012` | Projectile owns only its movement integration, transient world-space state, lifetime/termination, and collision-query execution. It reports neutral query/result facts for Combat Resolution. | Combat Gameplay | Future Projectile state initialized from a Shot Sample; query inputs use registered physics filters | Future Projectile simulation owner writes only Projectile state | Hit Query and Combat Resolution | From creation until termination/result handoff | `Planned` | Fire permission, targeting, reload, Dispersion, Exposure, damage, Integrity/lifecycle mutation, VFX/audio/UI, or any ship/Movement write authority. | Approved Pre-Phase 7 architecture freeze; exact simulation and result schema remain undesigned. |
| `CMB-CONV-013` | Hit resolution follows the one-way handoff `Projectile result -> Hit Query -> Combat Resolution`. Projectile must never directly modify Ship Integrity. | Combat Gameplay / Combat Resolution boundary | Future neutral Projectile result, registered Combat Geometry query result, and future Combat rules | Projectile writes only its result; future Hit Query derives hit context; future Combat Resolution alone owns outcome decisions | Damage/Integrity, lifecycle, Combat VFX event output, Basic Combat AI | Per completed Projectile query/result | `Planned` | A physics callback as damage authority, a VFX callback as Hit/Miss authority, or direct target mutation by Projectile. | Approved Pre-Phase 7 architecture freeze; exact ordering details and types remain undesigned. |
| `CMB-CONV-014` | Damage/Integrity resolution consumes hit information and target identity without depending on Renderer, Mesh, VisualRoot, or render bounds. Only a future registered damage-resolution owner may write Integrity and derived Combat lifecycle state. | Combat Resolution | Future resolved hostile hit context, target identity, semantic Combat Hull Region when available, and approved damage modifiers | Future registered damage-resolution owner only | Ship Integrity, Combat lifecycle, Combat VFX event output, Basic Combat AI | Per valid hostile resolution; state persists per ship | `Planned` | Renderer/Mesh coupling, per-region hit points, Projectile authority to damage, friendly-fire damage in Foundation, or permission to change Movement state. | Approved Pre-Phase 7 architecture freeze; damage model and Integrity scale remain undesigned/playtest-bound. |
| `CMB-CONV-015` | Auto Target scoring consumes stateless Target Exposure through `ART-API-014` / `ART-STA-001` as read-only projected geometry for each candidate already accepted by Fire Eligibility. | Combat Gameplay / Combat Art boundary | Active Target Exposure query/value, `CMB-STA-006`, and `CMB-STA-008` | Combat Art calculates Exposure; `CMB-API-008` creates only an immutable score value | Known: Phase 3B scorer/tests and Phase 3C selector. Planned: Auto Fire execution and Basic Combat AI | Per explicit candidate query; no cadence, cache, current target, or update loop | `Active` | Querying `Renderer.bounds`, Mesh, VisualRoot, acquiring/selecting an illegal target, writing Exposure/art data, or steering Movement. | `ShipAutoTargetScorer`; `AutoTargetScoreResult`; Phase 3B legality, exposure, dependency, and no-write tests; Phase 3C composition tests. |
| `CMB-CONV-016` | Dependencies flow `Combat Art -> Combat Foundation -> Combat Resolution`; resolved presentation facts may then flow to Combat Art/VFX receivers without granting VFX feedback authority. | Architecture boundary | This registry plus Active Movement and Combat Art contracts | Architecture/Registry change only | All future Combat implementations | Foundation invariant | `Planned` | Reverse dependencies from Art/VFX into gameplay decisions, Combat-owned art mutation, or Combat Resolution bypassing Foundation query/sample boundaries. | Approved Pre-Phase 7 architecture freeze; implementation evidence: none. |
| `CMB-CONV-017` | Auto Target selection is deterministic independently within each `CombatSide`, without adding tactical preference: higher `CMB-STA-008` FinalScore wins; an exact score tie prefers shorter `CMB-STA-006` distance; an exact score-and-distance tie preserves the earlier index in the caller-supplied candidate list. | Combat Gameplay | Active immutable score, eligibility result, candidate input, and per-side selection results | `ShipAutoTargetSelector` compares transient values only within the authoritative side; callers own candidate ordering | Auto Target callers and future Basic Combat AI | Reapplied from scratch on every explicit query; no retained target advantage | `Active` | Cross-side competition, strategic priority, flagship/unit-value weighting, stable global entity identity, target stickiness, or permission to reorder/discover candidates. | `ShipAutoTargetSelector.IsBetter`; Phase 3E Port/Starboard score, distance, and input-order tests. |
| `CMB-CONV-018` | When Blind Fire is not armed, a normal player right-click preserves Phase 3 precedence: a distinct clicked Combat Root may become Manual Target; otherwise the existing Destination/formation path runs. An armed Blind Fire world click takes precedence over both paths and is governed separately by `CMB-CONV-020`. Manual targeting never commands Heading, Destination, Tack, Wear, Root pose, or Formation. | Combat Gameplay / Command / Movement boundary | Active selection, `CMB-REF-004`, `CMB-API-010`, `CMB-API-014`, and existing Movement commands | `ShipPlayerCommandInput` chooses only the input route; registered Combat/Movement owners retain all state writes | Player command input, Manual Target, Blind Fire adapter, Editor diagnostics | Per completed right-click according to current one-shot arming state | `Active` | Fleet Blind Fire, target pursuit, faction validation, duplicated eligibility, or replacement of ordinary navigation/formation input. | `ShipPlayerCommandInput.CommitNormalDestinationClick`; Phase 3D regressions and Phase 4C precedence/fallthrough tests. |
| `CMB-CONV-019` | Auto Target candidate discovery is external to selection. A caller may supply any number of spatially relevant candidates, preferably already filtered to the shooter's current Maximum Range when a future provider can do so from authoritative data. `ShipAutoTargetSelector` imposes no gameplay count limit, performs no scene/entity/faction discovery, and does not duplicate the authoritative range calculation merely to prefilter. From that pool it returns at most one Port target and at most one Starboard target. | Combat Gameplay / future candidate-provider boundary | Active explicit `IReadOnlyList<AutoTargetCandidate>`, `CMB-API-007`, and `CMB-API-009`; no candidate-provider Source of Truth exists yet | Callers own transient list construction/order and relationship facts; selector only reads and derives immutable results | Known: Phase 3E explicit callers/tests and Phase 3F Editor dynamic debug list. Planned: registered spatial/relationship candidate provider and Basic Combat AI | One caller-supplied list per explicit query; no cache, retained membership, global scan, or persistent target ownership | `Active` | A global ship registry, faction system, spatial grid, selector-owned discovery, fixed two-slot API, queued/reloading reservation, or permission to duplicate Maximum Range semantics. | `ShipAutoTargetSelector.TrySelect`; Phase 3E arbitrary-count/signature/no-discovery tests; Phase 3F `ShipTestPanel` dynamic-list forwarding and no-discovery tests. |
| `CMB-CONV-020` | Foundation player Blind Fire is armed for one next valid right-click World/sea point and requires exactly one active selected controllable Combat ship. The attempt is consumed whether accepted or rejected and never also assigns Manual Target, dispatches Destination, or starts formation placement. Selection membership change or explicit cancellation clears unused arming. No production hotkey is assigned; the Editor Ship Test Panel is the temporary arming surface. | Combat Gameplay / Command input boundary | Active `CMB-STA-022`, `CMB-API-014`, selection membership, and current World navigation-plane click resolution | `ShipPlayerCommandInput` alone writes input arming/result state; Phase 4A/4B remain eligibility/action authority; Movement owners retain every Movement write | Foundation player/debug verification and future production HUD adapter | Transient one-shot input state; each valid click yields at most one attempted command | `Active` | `ShipCombatState`, a third target mode, multi-ship broadcast, held/repeating fire, a reserved keyboard binding, duplicated side/arc/range/reload logic, or Movement permission. | `ShipPlayerCommandInput`; Phase 4C selection, precedence, one-shot cleanup, delegation, and no-write tests. |

## Combat contract groups

Active entries identify their implemented symbols and access paths. All other
rows remain semantic plans and are not speculative API definitions.

| ID | Contract group | Kind | Owner | Purpose / Meaning | Unit | Coordinate Space | Source of Truth | Writable By | Known / Planned Consumers | Lifecycle | Status | Do Not Interpret As | Evidence |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `CMB-STA-001` | `ShipCombatState` | `MonoBehaviour` component and shared runtime state | Combat Gameplay | The single authoritative Combat Gameplay state owner for one ship. It resides on the authoritative Ship Root and owns Auto Fire enabled, nullable Manual Target, and both independent broadside reload states/timers. Blind Fire is intentionally not retained as owner state. `[DisallowMultipleComponent]` prevents a competing owner on that Root. | Mode/state identity `Scalar / N/A`; Manual Target is a Unity object reference; reload remaining values `s` | Mode/identity `Scalar / N/A`; broadside side uses ship semantics without storing spatial data | Private state fields on the one component instance | `CMB-API-001`, `CMB-API-002`, and `CMB-API-006` perform commands; only the component's `Update` advances reload timers; `CMB-API-013` composes those owner commands without direct field writes | Known: `CMB-PFB-001`, eligibility/selection/player commands, `CMB-API-013`, and Phase 1–4B tests. Planned: Auto/Manual firing execution, UI, AI, and projectile-launch logic | Runtime per ship; mode defaults are disabled/null; both broadsides default Ready at `0 s`; owner commands/tick alone mutate retained state | `Active` | Movement state, Combat Art data, another Root, persistent Auto Target, Blind Fire mode/current aim, per-cannon state, fire legality by itself, projectile creation, or a generic state framework. | `ShipCombatState.cs`; Phase 1/2 state tests; Phase 3 arbitration/selection/player-command tests; Phase 4A/4B query/action tests; Gelderland and generic-proxy tests. |
| `CMB-STA-002` | `ShipCombatState` Port / Starboard Broadside Reload State | Shared runtime state | Combat Gameplay | Two independent state/timer pairs. Each side is `Ready` at `0 s` or `Reloading` with a non-negative remaining duration. Getter-only properties expose Port and Starboard state/time separately. | State: `BroadsideReloadState`; remaining time: `float` in `s` | State/time `Scalar / N/A`; side identity follows `CombatSide` | Private `portBroadsideState` / `portReloadRemainingSeconds` and `starboardBroadsideState` / `starboardReloadRemainingSeconds` | `CMB-API-006` starts only the selected side; `CMB-API-013` requests one commit only after fresh eligibility; private owner `AdvanceReloads(float)` decrements each Reloading side | Known: target-aware/Blind Fire eligibility, Phase 4B Blind Fire action, tests, and Gelderland prefab. Planned: Auto/Manual firing execution, UI, Basic Combat AI | Runtime per ship and per side; initialized Ready/`0 s`; repeated/rejected commit never changes either timer; each timer advances/completes independently | `Active` | Final Fire Eligibility by itself, physical projectiles, shared ship-wide reload, per-cannon reload, half broadside, muzzle identity, VFX, or external timer writes. | `ShipCombatState.cs`; independent reload tests; Phase 2B/4A reads; Phase 4B commit/rejection/opposite-side isolation tests; prefab integration tests. |
| `CMB-STA-003` | `ShipCombatState.AutoFireEnabled` | Combat mode/state | Combat Gameplay | Getter-only flag recording whether Auto Fire mode is enabled. Enabling it clears `CMB-STA-004`; disabling it does not invent or clear a Manual Target. Phase 4B disables it only after successful Blind Fire broadside commit and never auto-restores it; rejected Blind Fire leaves it unchanged. | `Scalar / N/A` | `Scalar / N/A` | Private `ShipCombatState.autoFireEnabled` | `ShipCombatState.SetAutoFireEnabled(bool)` and successful `AssignManualTarget(GameObject)` paired transition; `CMB-API-013` calls the false command only after commit success | Known: selector/player commands and Phase 4B action. Planned: Auto Fire execution, UI, Basic Combat AI | Defaults `false`; retained until an explicit registered command changes it | `Active` | Global target discovery, automatic firing, legal-target proof by itself, eligibility failure as mode mutation, auto-restore after Blind Fire, broadside readiness, or Heading control. | `ShipCombatState.cs`; Phase 3 arbitration/selection/player tests; Phase 4A isolation; Phase 4B success/rejection tests. |
| `CMB-STA-004` | `ShipCombatState.ManualTarget` | Nullable target-identity state | Combat Gameplay | Getter-only nullable reference to a distinct authoritative Combat ship Root `GameObject` carrying `ShipCombatState`. Successful assignment disables `CMB-STA-003`; explicit clear stores null without enabling Auto Fire. Blind Fire is a separate one-shot action and preserves this reference on both success and rejection. | Unity `GameObject` reference; nullable | Target identity is `Scalar / N/A`; any later spatial reads use the target's registered Root in `World Space` | Private `ShipCombatState.manualTarget` | Accepted `AssignManualTarget` / `ClearManualTarget`; explicitly enabling Auto Fire clears it through `CMB-API-001`; `CMB-API-013` never writes it | Known: selector/player command and Phase 4B preservation tests. Planned: Manual Target fire behavior, UI, Basic Combat AI | Defaults null; retained through eligibility failure and Blind Fire actions; explicit clear or Auto Fire enable removes it | `Active` | Blind Fire aim storage, Port/Starboard Manual Targets, Auto-selected target storage, full eligibility, pursuit/steering, or a broad target abstraction. | `ShipCombatState.cs`; Phase 3 validation/arbitration/input tests; Phase 4B success/rejection preservation tests. |
| `CMB-STA-005` | Former `ShipCombatState.BlindFireEnabled` | Retired placeholder mode/state | Combat Gameplay | Phase 1 placeholder flag was removed because approved Phase 4 semantics define Blind Fire as a per-command action, not retained target-ownership mode. Replacement: `CMB-STA-019` through `CMB-STA-021` and `CMB-API-013`. | `Scalar / N/A` | `Scalar / N/A` | No runtime Source of Truth remains | Nobody | Historical Registry trace only | Removed in Phase 4B; no serialized field, property, or retained lifecycle | `Removed` | Permission to recreate a toggle, current Blind Fire mode, retained aim, or competing targeting state. | `ShipCombatState` no longer contains the field/property; regression tests reject the obsolete surface. |
| `CMB-STA-006` | `FireEligibilityResult` / final Foundation Fire Eligibility | Immutable derived query result | Combat Gameplay | The single authoritative target-aware Phase 2 explanation and `CanFire` decision. It exposes structural/caller relationship target legality, nullable applicable `Side`, arc/bearing, direct Root-to-Root distance, Maximum/Effective Range flags, applicable broadside `ReloadReady`, the current lifecycle bridge, whether an obstruction path was validly sampled, its exact origin/destination, obstruction/blocker identity, and combinable `FireEligibilityFailure` flags. `CanFire` requires TargetLegal, a valid side in arc, WithinMaximumRange, ReloadReady, LifecycleAllowsFire, and not Blocked. Effective Range is informational only. | Root/obstruction positions and distance `m`; bearing `deg`; side/booleans/failure flags/object identity `Scalar / N/A` | Target Root/distance and valid obstruction endpoints in `World Space`; signed bearing in `Ship Root Local Space` after removing World Y | One successful `CMB-API-007` evaluation over the current authoritative Root pose, target Root identity, caller relationship fact, `CMB-STA-002`, `CMB-STA-007`, `CMB-FLD-001`, and `CMB-FLD-002` | `ShipFireEligibility` constructs the readonly value; consumers receive value copies and cannot mutate it | Known: Phase 2 tests, Phase 2C Editor-only `ShipTestPanel`, Phase 3B scorer, and Phase 3C selector. Planned: Auto Fire execution, Manual Target fire, and Basic Combat AI | Stateless transient value per query; no cached current target/result and no update loop | `Active` | Blind Fire eligibility (`CMB-STA-020` is separate), fire execution, an allegiance Source of Truth, lifecycle state, hit probability, Auto Target Score by itself, Projectile, damage, or Effective Range as a hard rejection. | `FireEligibilityResult.cs`; `ShipFireEligibility.TryEvaluate`; Phase 2 eligibility/debug tests; Phase 3B legal-candidate tests; Phase 3C selection/invalidation tests; Phase 4A regression test; `ShipTestPanel`. |
| `CMB-STA-007` | Phase 2 minimal obstruction / `HasObstructionPath`, `ObstructionOriginWorld`, `ObstructionDestinationWorld`, `Blocked`, `BlockingCollider`, `BlockingObject` | Derived query state | Combat Gameplay | Reports whether a structurally valid target supplied an evaluated center-to-center path, its exact endpoints, whether its first deterministic qualifying collider blocks Foundation fire, and that collider/object identity for diagnostics. The query sorts all qualifying hits by distance and Unity Entity ID, then ignores colliders in the shooter and target hierarchies. | Endpoint/distance values `m`; validity/blocked/object identity `Scalar / N/A` | Valid path endpoints and hits in `World Space`; endpoints are shooter/target `ART-REF-004` CenterReference positions sampled by the query | `Physics.RaycastAll` filtered exclusively by `ART-LYR-001` layer index 8 / `PRJ-CONV-005`, explicitly using `QueryTriggerInteraction.Collide` | `ShipFireEligibility` derives the read-only result; no physics, Transform, art, or Combat state is mutated | Known: `CMB-API-007`, Phase 2B PlayMode tests, and Phase 2C Editor-only `ShipTestPanel`. Planned: Basic Combat AI; Phase 9 refinement | Evaluated on demand per final eligibility query; the Phase 2C debug consumer explicitly synchronizes moved transforms before its query, and validity/endpoint copies remain part of the transient result | `Active` | A second debug Physics query, valid endpoints for a structurally invalid target, per-cannon line of fire, Render Mesh/generic `ShipCollider` truth, island/fort coverage on unregistered layers, allegiance classification, friendly-fire damage, penetration, partial obstruction, or the final Phase 9 system. | `ShipFireEligibility.TryFindBlockingCollider`; clear/between/behind/filter/explicit-trigger/blocker-identity tests; debug endpoint/read-only tests; `ART-LYR-001`; `PRJ-CONV-005`. |
| `CMB-STA-008` | `AutoTargetScoreResult` / Auto Target Score | Immutable derived query result | Combat Gameplay | Explainable score for one candidate already accepted by `CMB-STA-006`. `Selectable` is true only for a successful query; `ExposureNormalized`, `RangeQualityNormalized`, and `FinalScore` are finite `[0,1]` values. `FinalScore = ExposureNormalized * RangeQualityNormalized` with no hidden modifiers. | Normalized factors and score `Scalar / N/A`; source Exposure area `m²`; source distance/ranges `m` | Result `Scalar / N/A`; source observer and Exposure geometry in `World Space` | One successful `CMB-API-008` call using the supplied legal `FireEligibilityResult`, Active `CMB-REF-001`, and Active `CMB-FLD-002` | `ShipAutoTargetScorer` constructs the readonly value; consumers receive value copies and cannot mutate it | Known: `CMB-API-009` Phase 3C selection. Planned: Auto Fire execution, Basic Combat AI, debug tooling | Stateless transient per explicit candidate query; illegal/invalid inputs return `false` and default non-selectable result | `Active` | Fire Eligibility, target discovery, accuracy/hit probability, HP/Integrity, flagship/unit value, raking, crew state, firing, mutable score state, or tie-breaking by itself. | `AutoTargetScoreResult.cs`; `ShipAutoTargetScorer.cs`; Phase 3B scoring tests; Phase 3C highest-score tests. |
| `CMB-REF-001` | Exposure consumer | Cross-system reference/data dependency | Combat Gameplay | Requests one immutable projected rectangle from the target's Active `ShipExposureReference` using the shooter Root World position as observer. It normalizes area by `ReferenceLength * ReferenceHeight` and never caches or mutates current-observer/current-Exposure state. | Width/height/reference dimensions `m`; area/reference area `m²`; normalized exposure `Scalar / N/A` | Observer and returned projection data in `World Space`; dimension semantics originate in `Ship Root Local Space` | `ART-API-012`, `ART-API-014`, `ART-STA-001`, and `ART-CONV-002` | Combat Art derives geometry; `ShipAutoTargetScorer` owns only returned value copies and its normalized scalar | Known: `CMB-API-008` and Phase 3B tests. Planned: future aim/fire-solution logic and debug visualization | Stateless query per explicit shooter/candidate pair | `Active` | Projectile Dispersion, Combat Geometry/bounds, writable art data, `Renderer.bounds`, Mesh/VisualRoot data, distance shrinking, accuracy, hit resolution, or damage. | `ShipAutoTargetScorer.TryEvaluate`; Phase 3B normalization, broadside/bow, Root-pose, renderer-free, and no-write tests; Active ART contracts. |
| `CMB-STA-009` | Dispersion | Combat-owned derived/configured state | Combat Gameplay | Produces weapon/crew/technology shot error from a future aim basis and Combat-owned configuration, independently of Exposure, Combat Geometry, and rendering. The output influences a Shot Sample launch direction or equivalent initial velocity but does not decide Hit/Miss. | Angular/linear units TBD explicitly at design | `Shot / Dispersion Plane Space`, with any conversion to `World Space` explicit | Future Combat dispersion configuration/calculation; never Exposure or collider/render dimensions | Future Combat fire-resolution/shot-sampling owner | Shot Sample generation and debug visualization | Evaluated independently per participating cannon/shot | `Planned` | Target geometry, collision geometry, target scoring, damage, an Exposure mutation, or a Hit/Miss result by itself. | Approved Phase 0.5 / Change 3 and Pre-Phase 7 architecture freeze; exact curves remain playtest-bound. |
| `CMB-STA-010` | Shot Sample | Per-shot runtime value | Combat Gameplay | One immutable launch-state snapshot for one participating cannon, created only after eligibility and participation are decided. It carries source/muzzle-side identity, copied socket-derived World origin, sampled World launch direction or equivalent initial-velocity information, and Foundation Round Shot identity; the exact schema is TBD. | Position `m`; direction `Scalar / N/A` or velocity `m/s`; identity `Scalar / N/A` | Launch origin and final launch direction/velocity in `World Space`; dispersion-space conversion must be explicit | Future authoritative shot sampler using the fire solution, `CMB-STA-009`, and a selected socket from `ART-SCK-001` / `ART-SCK-002` | Future Combat shot-sampling owner constructs once; downstream consumers read value copies | Projectile creation and diagnostics | Immutable transient per participating cannon | `Planned` | A mutable Transform/socket reference, required target identity, eligibility/reload state, Exposure/Combat Geometry ownership, Hit/Miss, damage, Projectile lifetime, VFX, UI, a whole-broadside aggregate, or per-pellet sample. | Approved Phase 0.5 / Change 3 and Pre-Phase 7 architecture freeze; exact type/API remain undesigned. |
| `CMB-STA-011` | Projectile | Runtime entity/state | Combat Gameplay | Foundation Round Shot simulation initialized from a Shot Sample. It owns movement integration, transient World position/velocity, lifetime/termination, and collision-query execution, then reports a neutral result for Hit Query/Combat Resolution. | Position `m`, velocity `m/s`, time `s`; further fields TBD | `World Space` | Future authoritative Projectile simulation initialized from `CMB-STA-010`; ship-hit queries use `ART-LYR-001` and `ART-API-008` | Future Projectile owner writes only Projectile state | Hit Query and Combat Resolution | Runtime transient from creation to termination/result handoff | `Planned` | Fire Eligibility, targeting, reload, Dispersion/Exposure ownership, damage, Integrity/lifecycle mutation, VFX/audio/UI, Chain/Grape Shot, or ship/Movement writes. | Approved Pre-Phase 7 architecture freeze; exact implementation, collision mechanics, and result schema remain undesigned. |
| `CMB-STA-012` | Projectile Result / Hit Query / Hit-Miss Resolution | Resolution handoff and derived result | Combat Gameplay / Combat Resolution | Preserves the one-way boundary: Projectile reports a neutral result; Hit Query identifies relevant collision/target/semantic region context; Combat Resolution authoritatively classifies Hit/Miss and applies Foundation rules before presentation. Exact payload and sequencing details are TBD. | `Scalar / N/A`; spatial result fields use `m` and declared vector semantics | Result position/normal in `World Space`; semantic region identity is `Scalar / N/A` | Future Projectile result plus registered Combat Geometry resolution through `ART-API-008`; authoritative outcome belongs to future Combat Resolution | Projectile writes only its result; Hit Query derives context; future Combat Resolution writes the outcome | Damage/Integrity, Raking, Combat VFX event output, Basic Combat AI | Runtime transient per completed query/result | `Planned` | Direct Integrity mutation by Projectile, VFX-owned Hit/Miss, Render Mesh hit resolution, Shot Sample alone, or guaranteed damage on every Hit. | Approved Pre-Phase 7 architecture freeze; exact types/API remain undesigned. |
| `CMB-STA-013` | Damage / Ship Integrity | Resolution service boundary and shared runtime state | Combat Resolution | Consumes resolved hostile hit information and target identity, optionally including semantic Combat Hull Region and approved modifiers, then updates the authoritative Foundation durability state. It has no Renderer, Mesh, VisualRoot, or render-bounds dependency. | Scale/unit TBD | `Scalar / N/A`; input spatial facts retain their registered spaces | Future authoritative Integrity state plus resolved hit/target context from `CMB-STA-012` | Future registered damage-resolution owner only | Combat lifecycle state, Combat VFX event output, Basic Combat AI, later HUD | Runtime state per ship, changed only by valid resolution | `Planned` | Projectile-owned damage, friendly-fire damage in Foundation, per-region hit points, Crew/rigging/fire/flooding, Renderer/Mesh/VisualRoot dependency, Movement mutation, or a frozen health scale. | Approved Phase 0.5 / Change 3 and Pre-Phase 7 architecture freeze; implementation evidence: none. |
| `CMB-STA-014` | Operational / Combat Disabled / Sinking | Combat lifecycle state | Combat Gameplay | Planned high-level ship combat lifecycle categories driven by future Integrity/sinking rules. Transition details are TBD and playtest-bound. | `Scalar / N/A` | `Scalar / N/A` | Future authoritative Combat lifecycle state | Future registered damage/lifecycle owner | Fire Eligibility, targeting, Combat VFX, Basic Combat AI, later HUD | Runtime per ship; terminal/reset behavior TBD | `Planned` | Movement destruction/ownership, final sinking simulation, or a detailed critical-state model. | Approved Phase 0.5 / Change 3 architecture; implementation evidence: none. |
| `CMB-STA-015` | Raking | Derived combat result/modifier | Combat Gameplay | Planned detection/modification for longitudinally advantageous fire. Exact geometry test and multiplier are TBD and playtest-bound. | Multiplier `Scalar / N/A`; angles `deg` if used | Explicit `World Space` to `Ship Root Local Space` relation | Future Combat raking evaluator using Movement Heading and Combat Geometry | Future Combat fire/damage-resolution owner | Hit/damage resolution, Combat VFX/debug output, Basic Combat AI | Evaluated per eligible fire/hit as designed | `Planned` | A frozen multiplier, per-gun arc, automatic target legality, or Heading control. | Approved Phase 0.5 / Change 3 architecture; implementation evidence: none. |
| `CMB-STA-016` | `AutoTargetCandidate` | Immutable caller-supplied candidate input | Combat Gameplay boundary | Contains exactly the candidate authoritative Ship Root and the caller's current `RelationshipAllowsFire` fact. Any number may appear in the caller's `IReadOnlyList`; there is no two-slot or other gameplay count limit. This is the minimum temporary bridge because no registered ship-entity enumeration, spatial candidate provider, or faction/allegiance Source of Truth exists. A future provider should prefer spatially relevant candidates within authoritative Maximum Range where it can do so without duplicating Phase 2 semantics. | Object identity and boolean `Scalar / N/A` | Root identity `Scalar / N/A`; subsequent spatial reads use registered Root/Art spaces | The explicit caller-provided candidate list; the Root is checked by Active Fire Eligibility and the relationship fact is not inferred | Constructed once per value by the caller; the selector receives value copies and writes neither field nor target | Known: `CMB-API-009`. Planned: replacement/provider from future registered entity, spatial-query, and relationship infrastructure | Transient per explicit selection query; no global discovery, fixed count, cache, retained membership, or ownership transfer | `Active` | A faction system, authoritative entity registry, tags/names, globally discovered ship list, guaranteed target legality, target priority, queued target, or mutable CurrentAutoTarget. | `AutoTargetCandidate.cs`; Phase 3E arbitrary-count, relationship rejection, immutability, and no-discovery tests. |
| `CMB-STA-017` | `AutoTargetSelectionResult` | Immutable transient aggregate selection result | Combat Gameplay | Contains independent getter-only `PortSelection` and `StarboardSelection` values plus `HasAnyTarget`. Default means neither side has a selection. It intentionally has no ambiguous ship-wide Target property. Each side may contain at most one target through `CMB-STA-018`; both may be populated simultaneously. | Aggregate flags/value copies `Scalar / N/A`; copied eligibility distance `m` | Aggregate identity `Scalar / N/A`; child eligibility spatial fields preserve their registered spaces | One `CMB-API-009` evaluation over the current explicit candidates and upstream query results | `ShipAutoTargetSelector` constructs the readonly value; callers receive a value copy; no shared CurrentAutoTarget field exists | Known: Phase 3E callers/tests and Phase 3D Editor-only `ShipTestPanel`. Planned: Auto Fire execution adapter and Basic Combat AI | Transient snapshot per call; next call replaces it at the caller boundary or returns default when no candidate remains legal | `Active` | `ShipCombatState.ManualTarget`, one ship-wide Auto Target, a multi-target list per side, persistent target ownership, target stickiness, fire permission retained across frames, firing/reload commit, or target discovery. | `AutoTargetSelectionResult.cs`; Phase 3E per-side/immutability tests; Phase 3D debug read/no-mutation test updated for both sides. |
| `CMB-STA-018` | `AutoTargetSideSelectionResult` | Immutable transient per-side selection result | Combat Gameplay | Reports `HasTarget` and, when selected, exactly one target Ship Root plus the corresponding authoritative Fire Eligibility snapshot, Auto Target score, and source candidate index. The containing property supplies Port or Starboard identity; the eligibility snapshot must report the same authoritative side. Default means no target for that side. | Identity/index/flags/score `Scalar / N/A`; copied distance `m` | Result identity/index `Scalar / N/A`; copied eligibility spatial fields preserve their registered spaces | Best legal candidate for one authoritative `CombatSide` during one successful `CMB-API-009` pass | `ShipAutoTargetSelector` constructs the readonly value; callers receive value copies | Known: `CMB-STA-017`, Phase 3E tests, and Editor-only `ShipTestPanel`. Planned: Auto Fire execution adapter and Basic Combat AI | Transient per side and query; no retained target, reservation, or reload queue | `Active` | Per-side Manual Target/mode, more than one selected target on a side, persistent ownership, queued target while reloading, firing, or reload commitment. | `AutoTargetSideSelectionResult.cs`; `ShipAutoTargetSelector.cs`; Phase 3E per-side, reload, tie-break, and immutability tests. |
| `CMB-STA-019` | `BlindFireAim` | Immutable targetless aim value | Combat Gameplay | Copies one valid normalized horizontal World aim direction and, only for point mode, the finite source World point plus direct Root-to-point distance. `HasWorldAimPoint` distinguishes point mode; direction mode has null point/distance and never invents a range. Default/invalid values are not valid aim bases. | Direction `Scalar / N/A`; point/distance `m` when present | Direction and optional point in `World Space`; World +Y is removed from direction only | One successful geometric resolution by `CMB-API-012` from an explicit caller value and authoritative shooter Root pose | `ShipFireEligibility` constructs the readonly value; consumers receive value copies | Known: `CMB-STA-020`, `CMB-API-013`, and Phase 4A/4B tests. Planned: Phase 6 fire solution before Dispersion/Shot Sample | Transient per explicit query/action; no retained Transform, target, or current aim state | `Active` | Target identity, Manual/Auto Target, Exposure, Renderer/Mesh/VisualRoot, mutable Transform, Dispersion, Shot Sample, Projectile, or permission to steer. | `BlindFireAim.cs`; Phase 4A value tests and Phase 4B accepted-aim preservation tests. |
| `CMB-STA-020` | `BlindFireEligibilityResult` / `BlindFireEligibilityFailure` | Immutable targetless eligibility result | Combat Gameplay | Explains valid aim, nullable side, shared broadside arc/bearing, point-range applicability and nullable Maximum Range verdict, applicable-side reload readiness, current lifecycle allowance, combinable failure reasons, and final `CanBlindFire`. `CanBlindFire` requires valid aim, side/in-arc, Maximum Range only when point mode applies, reload ready, and lifecycle allowed. | Optional point/distance `m`; bearing `deg`; flags/side `Scalar / N/A` | Aim data in `World Space`; bearing in `Ship Root Local Space` after removing World Y | One `CMB-API-012` evaluation using `CMB-STA-019`, `CMB-API-011`, `CMB-STA-002`, `CMB-FLD-001`, `CMB-FLD-002`, and the current always-allow lifecycle bridge | `ShipFireEligibility` constructs the readonly value; consumers receive value copies and no upstream state is mutated | Known: Phase 4A queries and fresh `CMB-API-013` execution revalidation. Planned: Blind Fire input/debug consumers | Stateless transient per call; command consumers must not treat an older snapshot as permission | `Active` | Target/relationship legality, Exposure, obstruction clearance, Effective Range rejection, fire/reload commit by itself, Dispersion, Shot Sample, Projectile, VFX, damage, or lifecycle state implementation. | `BlindFireEligibilityResult.cs`; Phase 4A tests; Phase 4B stale-result/reload revalidation tests. |
| `CMB-STA-021` | `BlindFireExecutionResult` / `BlindFireExecutionFailure` | Immutable accepted fire-intent / rejection result | Combat Gameplay | Reports whether one Blind Fire action was accepted, its nullable committed side, the freshly evaluated copied aim, the exact eligibility snapshot used, and combinable command-level failure reasons. Success means eligibility passed and the selected broadside reload commit succeeded; it does not mean a physical shot exists. | Optional point/distance `m`; direction/side/flags `Scalar / N/A` | Copied aim point/direction in `World Space`; nested bearing in `Ship Root Local Space` | One `CMB-API-013` execution and its immediate `CMB-API-012` revalidation/commit outcome | `ShipBlindFireCommand` constructs the readonly value; consumers receive value copies | Phase 4 debug/tests; planned Phase 6 fire-solution handoff | Transient per explicit action; no retained aim/target/result state | `Active` | Shot Sample, Projectile, Dispersion output, target identity, Exposure, Hit/Miss, damage, VFX event, or proof that a later physical launch occurred. | `BlindFireExecutionResult.cs`; `ShipBlindFireCommand.TryExecuteBlindFire`; Phase 4B/4B.1 focused EditMode tests. |
| `CMB-STA-022` | `ShipPlayerCommandInput.BlindFireArmed` | Transient input/UI state | Combat Gameplay / Command input | Getter-only one-shot flag indicating that the next valid right-click World/sea point will be consumed as one Blind Fire attempt. It is held on the command-input component, never on `ShipCombatState`. | `Scalar / N/A` | `Scalar / N/A` | Private `ShipPlayerCommandInput.blindFireArmed` | `CMB-API-014` arm/cancel/attempt paths and selection-membership cleanup only | Ship Test Panel and future production HUD input surface | Defaults false; set only after exact-one-selected validation; cleared on attempt, selection change, explicit cancel, or component disable | `Active` | Combat targeting mode, retained fire permission, repeated/held fire, per-ship state, or a keyboard binding. | `ShipPlayerCommandInput`; Phase 4C one-shot, selection, cancel, and assembly-boundary tests. |
| `CMB-STA-023` | `BlindFirePlayerCommandResult` / `BlindFirePlayerCommandFailure` | Immutable input-adapter result and last diagnostic snapshot | Combat Gameplay / Command input | Copies attempted shooter identity, requested World point, authoritative eligibility/execution snapshots when available, acceptance, and adapter-level failure reasons. `ShipPlayerCommandInput` retains only the most recent value for diagnostics; gameplay authority remains `CMB-STA-020` / `021`. | Point/distance `m`; direction/side/flags/identity `Scalar / N/A` | Requested point and nested aim in `World Space`; nested bearing in `Ship Root Local Space` | One `CMB-API-014` attempt delegating to `CMB-API-012` / `013` | `ShipPlayerCommandInput` replaces its private last-result copy; consumers receive readonly value copies | Editor Ship Test Panel and focused tests; future HUD may consume a returned value | One transient value per adapter attempt; replaced on next attempt and invalid when `Attempted == false` | `Active` | Alternate eligibility authority, Shot Sample, Projectile, target ownership, Movement command, telemetry persistence, or physical firing proof. | `BlindFirePlayerCommandResult.cs`; Phase 4C result/panel/immutability tests. |
| `CMB-EVT-001` | Combat VFX event output | Event/callback boundary | Combat Gameplay | Emits already-decided muzzle-fire, water-impact, and hull-impact spatial facts through the Active `ART-EVT-001` through `ART-EVT-004` / `ART-API-015` presentation boundary. Future Combat emission timing and dispatch remain undesigned. | Per Active ART event payload | Active ART event coordinate spaces; spatial values are `World Space` | Future authoritative Combat fire/projectile/resolution result; never the VFX receiver | Future Combat event producer constructs event values; VFX consumes them read-only | Combat Art/VFX | Runtime transient only after the corresponding authoritative decision | `Planned` | A new event bus/API, permission for VFX to decide Hit/Miss/Damage, an Integrity writer, or a Movement command channel. | Approved Phase 0.5 / Change 3 and Pre-Phase 7 architecture freeze over Active Combat Art VFX contracts; Combat producer evidence: none. |
| `CMB-REF-002` | Basic Combat AI consumer | Consumer boundary | Combat AI | Reads legal-target, score, broadside readiness, lifecycle, and Movement spatial contracts to choose Combat actions. Detailed AI is deferred. | `Scalar / N/A` | Reads registered spaces only | Registered Combat and Movement Sources of Truth | AI may issue only future registered Combat commands; never direct Movement/Combat-state writes | Basic Combat AI | Runtime decision cadence TBD | `Planned` | Advanced combat AI, Movement steering, rule ownership, or direct state mutation. | Approved Phase 0.5 / Change 3 architecture; implementation evidence: none. |
| `CMB-REF-003` | Fire Eligibility target/spatial identity dependency | Cross-system object/reference dependency | Combat Gameplay | A structurally legal Phase 2B target is a distinct authoritative ship Root carrying `ShipCombatState`, `ShipArtDefinition` with non-null `ART-REF-004` CenterReference, and `ShipCombatGeometry`. Arc/range use the target authoritative Root position to preserve Phase 2A semantics; broadside-level obstruction uses shooter and target CenterReferences. The separate caller boolean supplies only unresolved relationship legality because no faction/allegiance Source of Truth exists. | Root/center positions `m`; component/object identities and caller fact `Scalar / N/A` | Root and center positions in `World Space`; arc bearing conversion uses shooter `Ship Root Local Space` | `MOV-REF-001` target Root identity, Active Combat/Art components, `ART-REF-004`, and the current caller relationship fact | Movement and Combat Art retain their writers; `ShipFireEligibility` reads only; the caller owns only the per-query relationship fact | Known: `CMB-API-007`, `CMB-STA-006`, `CMB-STA-007`, Phase 2B tests | Resolved on every query; no retained target/reference/current-shooter state | `Active` | A faction system, hardcoded tag/name, target acquisition, Renderer/VisualRoot/mesh identity, permission to mutate any component/Transform, or a claim that component presence guarantees a fully validator-passing prefab. | `ShipFireEligibility.TryGetTargetSpatialIdentity`; null/self/missing-identity/caller-rejection tests; Active `CMB-STA-004`, `ART-REF-004`, and Combat Geometry contracts. |
| `CMB-REF-004` | Player-click authoritative Combat Root resolution | Cross-system object/reference dependency | Combat Gameplay / Command boundary | Resolves a clicked object at any depth beneath a Combat ship upward to the nearest active `ShipCombatState`, whose registered placement is the authoritative Ship Root. That Root—not the clicked child, VisualRoot, Renderer, Mesh, generic collider, or CombatGeometry child—is supplied to `CMB-API-002`. | GameObject/component identity `Scalar / N/A` | Hierarchy identity `Ship Root Local Space`; no spatial value is derived from child geometry | Active `CMB-STA-001` placement on `MOV-REF-001` | Prefab architecture owns component placement; `ShipPlayerCommandInput` resolves and reads only; `ShipCombatState` owns any accepted target write | Known: `CMB-API-010` and Phase 3D child-resolution tests | Resolved once per attempted normal right-click command; no retained child/collider reference | `Active` | Combat Geometry as target identity, faction/hostility proof, global target discovery, permission to move either Root, or a relaxation of Fire Eligibility's fuller spatial identity checks. | `ShipPlayerCommandInput.TryResolveCombatShipRoot`; Phase 3D root/child/non-Combat/self/multi-selection tests. |

## Active Combat APIs

| ID | Symbol | Kind | Owner | Access / Signature | Unit | Coordinate Space | Purpose / Meaning | Source of Truth | Writable By | Known / Planned Consumers | Lifecycle / Side Effects | Status | Do Not Interpret As | Replacement | Evidence |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `CMB-API-001` | `ShipCombatState.SetAutoFireEnabled(bool)` | Command API | Combat Gameplay | Public instance method | `Scalar / N/A` | `Scalar / N/A` | Sole explicit enable/disable command for `CMB-STA-003`. `true` atomically clears Manual Target and enables Auto Fire; `false` disables Auto Fire without creating or clearing a Manual Target. | Command argument plus current owner state | Callers request the transition; `ShipCombatState` alone writes private mode fields; `CMB-API-013` calls `false` only after successful commit | Known: Phase 1A/3A tests and Phase 4B action. Planned: Combat UI and Basic Combat AI adapters | Updated only on call; no target discovery, eligibility, event, Transform, Movement, or Combat Art side effects | `Active` | Automatic firing, a toggle, target legality, rejected-eligibility mode mutation, auto-restore, or permission to write fields directly | N/A | `ShipCombatState.SetAutoFireEnabled`; Phase 3 arbitration/isolation and Phase 4B success/rejection tests. |
| `CMB-API-002` | `ShipCombatState.AssignManualTarget(GameObject)` / `ClearManualTarget()` | Command API pair | Combat Gameplay | Public instance methods; assignment returns `bool` and accepts only a non-null, non-self Root carrying `ShipCombatState`; clear returns `void` | Reference/result `Scalar / N/A` | Target identity `Scalar / N/A` | Supported assign/clear paths for `CMB-STA-004`. Accepted assignment atomically replaces Manual Target and disables Auto Fire. Rejected assignment returns `false` without state change. Clear stores null and does not enable Auto Fire. | Command argument plus the candidate Root's `ShipCombatState` identity | Callers request transitions; `ShipCombatState` alone validates and writes its private mode fields | Known: Phase 1A/3A tests and `CMB-API-010` player-input adapter. Planned: Combat UI/AI command adapters | Evaluated only on call; no scene scan, Fire Eligibility requirement, steering, firing, event, reload write, Transform, Movement, Blind Fire, or Combat Art mutation | `Active` | Faction validation, complete `CMB-REF-003` spatial validation, target discovery, aim solution, input-hit resolution by itself, guaranteed `CanFire`, or permission to mutate the target | N/A | `ShipCombatState.AssignManualTarget`; `ClearManualTarget`; Phase 3A command tests; Phase 3D player command/invalidation tests. |
| `CMB-API-003` | Former `ShipCombatState.SetBlindFireEnabled(bool)` | Retired placeholder command API | Combat Gameplay | Removed with `CMB-STA-005`; approved replacement is the explicit per-action `CMB-API-013`. | `Scalar / N/A` | `Scalar / N/A` | No runtime API remains | Nobody | Historical Registry trace only | Removed in Phase 4B | `Removed` | Permission to recreate a persistent Blind Fire toggle or a second execution path. | N/A | `ShipCombatState` no longer exposes the method; state/panel regression tests reject the obsolete API. |
| `CMB-API-004` | `CombatSide` | Shared semantic enum | Combat Gameplay | Public enum with exactly `Port` and `Starboard` | `Scalar / N/A` | Ship-side semantic identity; no coordinate value is stored | Stable typed identity for selecting one broadside and identifying the applicable/committed side in eligibility and action results. | Supplied to `CMB-API-006` or derived by `CMB-API-007`, `CMB-API-012`, and fresh `CMB-API-013` evaluation; identity is immutable | Nobody mutates the type | Known: Phase 1B–4B APIs/tests. Planned: UI and Basic Combat AI | Static type contract; no side effects | `Active` | An arbitrary string, muzzle Transform identity, per-cannon identity, arc result by itself, or local-space vector. | N/A | `CombatSide`; independent-side, shared classification, and Blind Fire execution tests. |
| `CMB-API-005` | `BroadsideReloadState` and `ShipCombatState` broadside read properties | Shared enum and getter-only read API | Combat Gameplay | Public enum exactly `Ready` / `Reloading`; public getter-only Port/Starboard state and remaining-time properties | Remaining time `s`; state `Scalar / N/A` | `Scalar / N/A` | Read-only access to authoritative `CMB-STA-002` pairs | Private fields owned by `ShipCombatState` | Consumers receive enum/float values; `CMB-API-012` and `CMB-API-013` read them | Known: Phase 1B–4B tests. Planned: Auto/Manual firing, UI, Basic Combat AI | Read on demand; no side effects | `Active` | Complete fire permission, mutable snapshot, per-cannon state, external timer ownership, or duration setter. | N/A | `ShipCombatState.cs`; read-only, eligibility, action commit/rejection, and independence tests. |
| `CMB-API-006` | `ShipCombatState.TryCommitBroadsideFire(CombatSide)` | State-transition command API | Combat Gameplay | Public instance method returning `bool` | `Scalar / N/A`; resulting timer uses `s` | Side identity `Scalar / N/A` | Records an already-authorized broadside commitment by transitioning only the selected Ready side to Reloading and loading `CMB-FLD-003`. Reloading/invalid input fails without restarting/extending either timer. | Selected side state plus `CMB-FLD-003` | `ShipCombatState` alone writes state/timer fields; Active `CMB-API-013` is the first authoritative gameplay action caller | Known: Phase 1B tests and Phase 4B Blind Fire action. Planned: Auto/Manual fire paths | Updated only on call; no Shot Sample, Projectile, VFX, damage, Transform, Movement, or Combat Art side effects | `Active` | A legality query, physical firing, Projectile/VFX emission, timer reset, or half/per-cannon fire. | N/A | `ShipCombatState.TryCommitBroadsideFire`; Phase 1B transitions and Phase 4B execution/isolation tests. |
| `CMB-API-007` | `ShipFireEligibility.TryEvaluate(GameObject, bool, out FireEligibilityResult)` and read-only configuration properties | Stateless target-aware query API | Combat Gameplay | Public `bool TryEvaluate(GameObject targetShipRoot, bool targetRelationshipAllowsFire, out FireEligibilityResult result)` instance method on the shooter Root; public getter-only `ForwardArcLimitDegrees`, `AftArcLimitDegrees`, `EffectiveRangeMeters`, and `MaximumRangeMeters` | Target identity, relationship, flags, side, and blocker identity `Scalar / N/A`; distance/ranges/endpoints `m`; limits/bearing `deg` | Target Root, center positions, direct distance, and obstruction endpoints in `World Space`; horizontal direction converted to `Ship Root Local Space`; World +Y is ignored only for side/bearing | Evaluates the unified Phase 2 target-aware Foundation decision and explanation, including copies of the exact center-to-center obstruction endpoints. Returns `false` with default result when shooter/configuration inputs cannot support a query or when no horizontal target direction exists. Null/self/missing-spatial-identity targets are successful explained evaluations with TargetIllegal. The caller boolean is an explicit temporary relationship fact, not an allegiance owner. Broadside classification delegates to shared `CMB-API-011`; Blind Fire uses separate `CMB-API-012`. | Current shooter Root pose, target `CMB-REF-003` identity, caller relationship fact, applicable `CMB-STA-002` state, Active Art center/layer contracts, and `CMB-FLD-001` / `CMB-FLD-002` | Combat prefab/scene authoring writes serialized configuration; registered owners retain all upstream state writes; query/consumers are read-only | Known: Phase 2 tests, `CMB-PFB-002`, Editor-only `ShipTestPanel`, `CMB-API-008`, and `CMB-API-009`. Planned: Manual/Auto firing execution and Basic Combat AI | Evaluated on demand with no cached target/current result or update loop; performs one explicit filtered physics read but no Transform, Movement, reload, Combat Art, event, fire, Projectile, VFX, or damage writes | `Active` | Blind Fire eligibility, target discovery, an allegiance/faction Source of Truth, a fire command, lifecycle implementation, Auto Target score by itself, per-gun arc/line, Projectile, VFX, damage, or permission for consumers to recompute/override eligibility outcomes. | N/A | `ShipFireEligibility.cs`; Phase 2 rule tests; Phase 3B scoring-consumer tests; Phase 3C selection/invalidation tests; Phase 4A target-aware regression. |
| `CMB-API-008` | `ShipAutoTargetScorer.TryEvaluate(GameObject, GameObject, FireEligibilityResult, out AutoTargetScoreResult)` | Stateless query API | Combat Gameplay | Public static method receiving an explicit shooter Root, target Root, and corresponding authoritative eligibility result | Root identity/result/factors `Scalar / N/A`; observer position/distance/ranges `m`; Exposure area `m²` | Shooter observer and Exposure query in `World Space`; normalized result `Scalar / N/A` | Rejects any result whose `CanFire` is false. For valid explicit Roots, reads `ShipFireEligibility` ranges from the shooter and `ShipExposureReference` from the target, then calculates only the registered E, R, and E*R values. | Supplied matching `CMB-STA-006`, shooter `CMB-FLD-002`, and target Active `CMB-REF-001` | Registered upstream owners retain all writes; the static scorer creates one immutable value and writes no shared state | Known: `CMB-API-009`. Planned: Auto Fire execution, Basic Combat AI, debug tooling | On-demand only; returns `false` with default non-selectable result for illegal candidates, missing dependencies, invalid Exposure/dimensions/ranges, or non-finite inputs | `Active` | Candidate discovery, proof that the supplied result belongs to the supplied target, selection/tie-breaking by itself, target retention, firing, score caching, Combat/Art/Movement mutation, or advanced tactical AI. | N/A | `ShipAutoTargetScorer.cs`; `AutoTargetScoreResult.cs`; Phase 3B EditMode tests; Phase 3C composition tests. |
| `CMB-API-009` | `ShipAutoTargetSelector.TrySelect(GameObject, IReadOnlyList<AutoTargetCandidate>, out AutoTargetSelectionResult)` | Stateless per-side selection query API | Combat Gameplay | Public static method receiving the explicit shooter Root and an arbitrary-length caller-supplied immutable candidate list | Root identities, relationship flags, index, side, and score `Scalar / N/A`; eligibility distance `m` | Reads current registered Root/Art spaces through upstream APIs; returned identity/index/score is `Scalar / N/A` | Acts only when the shooter's Active `ShipCombatState` has Auto Fire enabled and ship-wide Manual Target null. In one pass, for each candidate in supplied order it rejects null/self identity, calls `CMB-API-007`, discards `CanFire == false`, calls `CMB-API-008`, and routes the candidate exclusively by authoritative eligibility `Side`. It independently retains at most one best Port and one best Starboard candidate using `CMB-CONV-017`; it returns default/false when inactive or when neither side qualifies. | Active `CMB-STA-003`, `CMB-STA-004`, `CMB-STA-006`, `CMB-STA-008`, `CMB-STA-016` through `CMB-STA-018`, and current upstream registered pose/Art inputs | Registered upstream owners retain every write; the selector creates only transient result values and owns no persistent CurrentAutoTarget | Known: Phase 3E explicit callers/tests and Phase 3D Editor-only debug consumer. Planned: Auto Fire execution adapter and Basic Combat AI | Re-evaluated from scratch per call; no update loop, count limit, object discovery, target retention, state mutation, steering, reload commit, Shot Sample, Projectile, or VFX | `Active` | A fire command, global entity/faction/spatial provider, Manual Target writer, per-side Manual mode, persistent target lock, tactical AI, relationship inference, duplicated range prefilter, or permission to reuse old eligibility across calls. | N/A | Phase 3E source/tests; `ShipTestPanel.TrySelectAutoTargetForDebug`; Phase 3D debug no-mutation test updated for both sides. |
| `CMB-API-010` | `ShipPlayerCommandInput.TryAssignManualTarget(GameObject)` and normal-click interception | Player command API / cross-system adapter | Combat Gameplay / Command input | Public `bool TryAssignManualTarget(GameObject clickedObject)` plus the private normal-click screen-hit adapter. The public command succeeds only for exactly one active selected `ShipDestinationController`, a co-located shooter `ShipCombatState`, and a distinct target resolved by `CMB-REF-004`; it delegates the write to `CMB-API-002`. The normal-click adapter performs one explicit `Physics.Raycast` against default raycast layers with `QueryTriggerInteraction.Collide`, consumes the click only on success, and otherwise executes the pre-existing destination path. | Object identity, selected-count, and success flag `Scalar / N/A`; screen ray/spatial hit in `World Space` | Input screen position converts through the command Camera to a `World Space` ray; stored target identity is the authoritative Root | Existing `ShipSelectionManager` single selection, camera hit object, `CMB-REF-004`, and `CMB-API-002` | Input selects the command route only; `ShipCombatState` exclusively writes Auto/Manual mode state; Movement command owners exclusively write destination/navigation/pose | Known: runtime right-click flow and Phase 3D tests. Planned: production input polish only if separately approved | Evaluated once on normal right-click completion; formation-placement drag path bypasses it; no per-frame scan/cache | `Active` | Fleet attack, faction relationship validation, Fire Eligibility query, firing, reload commit, target pursuit, Heading/pose control, selection mutation, or a replacement for existing destination/formation dispatch. | N/A | `ShipPlayerCommandInput.CommitNormalDestinationClick`, `TryAssignManualTargetAtScreenPosition`, `TryAssignManualTarget`; Phase 3D command/fallback/ownership tests. |
| `CMB-API-011` | `ShipBroadsideGeometry.TryEvaluate` / `HasValidArcConfiguration` | Internal pure shared geometry API | Combat Gameplay | `internal static` API shared by target-aware and Blind Fire eligibility; accepts authoritative Root Transform, a World direction, and forward/aft limits, then returns normalized horizontal World direction, nullable side, arc result, and signed local bearing through an internal readonly result. | Direction `Scalar / N/A`; limits/bearing `deg` | Input/output direction in `World Space`; signed bearing in `Ship Root Local Space`; World +Y removed | Current Root orientation plus Active `CMB-FLD-001`; one formula owns finite checks, normalization, dead zones, inclusive boundaries, and Port/Starboard classification | Pure derivation only; no Transform, Movement, Combat state, or configuration writes | Known: `CMB-API-007` internal target geometry path and `CMB-API-012` | Evaluated on demand; returns false/default for invalid configuration/root/direction and retains no state | `Active` | Movement-owned geometry, per-gun arc, a fire command, range/reload/target legality, aim correction, or permission to steer. | N/A | `ShipBroadsideGeometry.cs`; `ShipFireEligibility.TryEvaluateGeometry` and Blind Fire paths; Phase 2 regression plus Phase 4A boundary/rotation tests. |
| `CMB-API-012` | `ShipFireEligibility.TryEvaluateBlindFireAtPoint` / `TryEvaluateBlindFireDirection` | Stateless targetless eligibility query API | Combat Gameplay | Public instance methods accepting an explicit `Vector3` and `out BlindFireEligibilityResult`. They return false/default only when shooter configuration or required co-located state/Root pose cannot support evaluation. Point mode derives horizontal direction from current Root position and enforces inclusive Maximum Range; direction mode strips World Y and has no range verdict. | Point/distance `m`; direction/flags/side `Scalar / N/A`; bearing/arc config `deg` | Point/direction in `World Space`; bearing in `Ship Root Local Space` | Explicit caller aim, authoritative Root pose, shared `CMB-API-011`, `CMB-STA-002`, `CMB-FLD-001` / `002`, lifecycle bridge | Read-only query only; no reload, Auto Fire, Manual Target, Movement, or execution writes | Known: Phase 4A callers/tests and mandatory fresh revalidation by `ShipBlindFireCommand` inside `CMB-API-013`. Planned: debug/input | On demand; no cached aim/target/result or mutation | `Active` | Permission to commit reload by itself, stale authorization, stored Blind Fire mode/aim, obstruction, Dispersion, Shot Sample, Projectile, damage, AI, or UI. | N/A | `ShipFireEligibility.cs`; Phase 4A tests; Phase 4B/4B.1 fresh-revalidation and read-only ownership tests. |
| `CMB-API-013` | `ShipBlindFireCommand.TryExecuteBlindFire(BlindFireAim, out BlindFireExecutionResult)` | Authoritative targetless action command | Combat Gameplay | Public instance method on a stateless non-`MonoBehaviour` service constructed with the co-located `ShipFireEligibility` and `ShipCombatState`. Its `bool` return is `true` exactly when the action is accepted and the applicable broadside commit succeeds; every rejection returns `false` while the immutable out result carries detailed diagnostics. It validates the supplied aim, re-runs the matching point/direction `CMB-API-012` query from current Root/configuration/reload/lifecycle state, requires exactly one legal side, and calls `CMB-API-006` once. Only commit success disables Auto Fire through `CMB-API-001`; Manual Target is preserved. | Optional point/distance `m`; direction/side/result flags `Scalar / N/A` | Aim in `World Space`; eligibility bearing in `Ship Root Local Space` | Supplied immutable `CMB-STA-019`, fresh read-only `CMB-STA-020`, authoritative `CMB-STA-002`, and owner commands `CMB-API-006` / `CMB-API-001` | `ShipBlindFireCommand` coordinates the action and creates immutable `CMB-STA-021`; `ShipCombatState` remains sole retained-state writer; `ShipFireEligibility` writes no state | Known: `CMB-API-014` and Phase 4 tests. Planned: Phase 6 fire-solution handoff | One stateless service instance may execute atomic attempts. Rejection mutates no reload/mode/target/pose; success commits one side, disables Auto Fire until another command, and retains no current aim/result | `Active` | A required prefab component/hierarchy object, Shot Sample, Projectile, Dispersion, VFX event, damage, target acquisition, target-aware obstruction, Manual Target replacement, Heading control, or proof of physical launch. | N/A | `ShipBlindFireCommand.cs`; Phase 4B/4B.1/4C/4C.1 delegation, rejection, and ownership tests. |
| `CMB-API-014` | `ShipPlayerCommandInput.TryArmBlindFire` / `CancelBlindFire` / `TryExecuteBlindFireAtWorldPoint` and armed-click interception | Player command/input adapter API | Combat Gameplay / Command input | Public arm/cancel/direct-world-point methods plus private right-click routing. Arming requires exactly one active selected `ShipDestinationController` with active co-located `ShipCombatState` and `ShipFireEligibility`. A valid armed click consumes arming, resolves the existing navigation-plane World point, obtains its preview aim only through `CMB-API-012`, and delegates execution only to a `ShipBlindFireCommand` through `CMB-API-013`; accepted and rejected attempts never fall through to Manual Target, Destination, or formation dispatch. | World point `m`; selection/result/arming `Scalar / N/A` | Screen click resolves through existing command Camera/navigation plane to `World Space` | Active selection, existing world-click resolver, `CMB-STA-022`, `CMB-API-012`, and authoritative `CMB-API-013` | Adapter alone writes its input/debug state; the dedicated command coordinates Combat owner writes; Movement registered owners retain all Movement writes | Ship Test Panel temporary arm surface, player command tests, future production HUD | Arm persists until one valid click, selection change, cancel, or disable; each attempt creates a transient command service and produces `CMB-STA-023` | `Active` | Side/arc/range/reload calculation, direct Combat-state mutation, target acquisition, multi-ship broadcast, Movement/formation command, production key binding, Shot Sample, Projectile, VFX, or damage. | N/A | `ShipPlayerCommandInput`; Phase 4C/4B.1 selection, precedence, command-delegation, cleanup, and no-write tests. |

## Active Combat prefab integration

| ID | Contract | Kind | Owner | Purpose / Meaning | Access / Type | Unit / Space | Source of Truth | Writable By | Known / Planned Consumers | Lifecycle / Side Effects | Status | Do Not Interpret As | Replacement | Evidence |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `CMB-PFB-001` | Gelderland `ShipCombatState` integration | Prefab component contract | Combat Gameplay | `PF_Ship_Gelderland_Combat_v01` carries exactly one `ShipCombatState` on its inherited authoritative Ship Root. Generic Light/Medium/Heavy Movement proxies carry none. | Component added to the Gelderland prefab variant Root | `Scalar / N/A`; configuration duration uses `s` | Gelderland variant component plus inherited authoritative Root | Combat prefab authoring may tune temporary `CMB-FLD-003`; runtime state remains owner-written | Known: Phase 1B prefab/isolation tests and co-located `CMB-PFB-002` eligibility consumer. Planned: later Combat Gameplay consumers | Static prefab configuration; creates one runtime state owner when instantiated; does not change Root, Movement stack, Combat Art hierarchy/data, or generic proxies | `Active` | A second Combat/Movement Root, generic-proxy contamination, final balance, automatic fire integration, or permission to alter Combat Art. | N/A | Gelderland variant root component serialized with `30 s` temporary duration; exact-one-owner, evaluator-dependency, and generic-proxy isolation EditMode tests. |
| `CMB-PFB-002` | Gelderland `ShipFireEligibility` integration | Prefab component/configuration contract | Combat Gameplay | `PF_Ship_Gelderland_Combat_v01` carries exactly one `ShipFireEligibility` on the same authoritative Root as `ShipCombatState`, `ShipArtDefinition`, and `ShipCombatGeometry`. Generic Light/Medium/Heavy Movement proxies carry none. | Component added to the Gelderland prefab variant Root with `CMB-FLD-001` / `CMB-FLD-002` serialized values | Arc `deg`; ranges `m`; Root/center reads in `World Space` and arc bearing in `Ship Root Local Space` | Gelderland variant component plus inherited authoritative Root and Active Combat Art references/geometry | Combat prefab authoring may tune temporary arc/range values; runtime query is read-only | Known: Phase 2B prefab/isolation tests. Planned: later Combat entry points | Static prefab configuration; creates one runtime evaluator when instantiated; does not change Root, Movement stack, Combat Art hierarchy/calibration, generic proxies, or Combat state | `Active` | Final balance, automatic firing, a second Root, generic-proxy contamination, Art calibration ownership, or permission to modify reload/Movement. | N/A | Gelderland variant Root component serialized with temporary `80/70 deg`, `300/500 m`; exact-one-evaluator, dependency, configuration, and generic-proxy isolation EditMode tests. |

## Frozen ownership summary

| Concern | Frozen owner | Approved dependency / handoff | Explicitly outside the owner |
|---|---|---|---|
| Combat mode/target and broadside reload state | Active `ShipCombatState` on the authoritative Ship Root | Atomic Auto Fire / Manual Target command arbitration -> private mode state; broadside commands -> private reload state; owner `Update` -> independent timer progression; getter-only consumer reads | Target discovery, eligibility-driven mode mutation, Movement/Combat Art state, actual fire, per-cannon state, public state/timer writers, events, or a second Combat Root |
| Phase 2 Foundation Fire Eligibility | Active `ShipFireEligibility` query and immutable `FireEligibilityResult` | Root pose + `CMB-REF-003` target/caller fact + applicable `CMB-STA-002` read + center-to-center `CMB-STA-007` query + temporary arc/range configuration + current always-allow lifecycle bridge -> explainable `CanFire` and exact diagnostic endpoint copies; the Phase 2C Editor tool consumes this output only | Cached target/current result, any upstream write, debug-owned rule/Physics recomputation, allegiance ownership, real lifecycle state, selection, scoring, fire/reload commit, per-gun queries, Projectile, VFX, or damage |
| Phase 4A Blind Fire eligibility | Active read-only `ShipFireEligibility` targetless queries plus immutable `BlindFireAim` and `BlindFireEligibilityResult` | Explicit point/direction + current Root pose/shared geometry/reload/range/lifecycle reads -> explainable `CanBlindFire`; no cached authorization or state mutation | Fire/reload commit, Auto/Manual Target mutation, retained Blind mode/aim/result, Movement correction, Dispersion, Shot Sample, Projectile, VFX, or damage |
| Phase 4B/4B.1 Blind Fire action | Active stateless `ShipBlindFireCommand` plus immutable `BlindFireExecutionResult` | Supplied aim -> fresh `ShipFireEligibility` query -> exactly one `ShipCombatState` broadside commit -> Auto Fire disable only after commit; Manual Target preserved | Eligibility rule duplication, retained state, target acquisition/identity/relationship, Movement correction, Dispersion, Shot Sample, Projectile, VFX, damage, or physical launch proof |
| Phase 4C Blind Fire player-command adapter | Active `ShipPlayerCommandInput` one-shot arming, World-point adapter, and immutable diagnostic result | Exact-one active selected Combat ship + next valid navigation-plane right-click point -> Phase 4A preview aim query -> transient Phase 4B.1 command service; attempt consumes arming and intercepts Manual Target/Destination/formation paths | Direct Combat-state mutation, Combat rule duplication, multi-select broadcast, persistent Combat mode, production binding/HUD, Movement writes, physical firing, or Editor visualization authority |
| Phase 3B Auto Target Score | Active static `ShipAutoTargetScorer` and immutable `AutoTargetScoreResult` | Legal `FireEligibilityResult` + shooter Root observer/range configuration + target Active Exposure query -> clamped E and temporary R -> exact E*R score | Candidate discovery/selection, tie-breaking, target retention, accuracy/HP/raking/strategic modifiers, firing, update loop, or any upstream write |
| Phase 3C/3E Auto Target selection | Active static `ShipAutoTargetSelector`, immutable `AutoTargetCandidate`, `AutoTargetSideSelectionResult`, and `AutoTargetSelectionResult` | Arbitrary-length explicit caller list -> ship-wide Auto/Manual mode gate -> authoritative eligibility filter -> Active scorer -> exclusive authoritative-side routing -> independent deterministic Port and Starboard best selections, each at most one | Entity/faction/spatial discovery, relationship inference, duplicated range prefilter, persistent CurrentAutoTarget, per-side Manual mode, queued/reloading reservation, target stickiness, tactical weighting, Movement commands, firing/reload commit, Projectile, VFX, or any upstream write |
| Phase 3D player Manual Target command | Active `ShipPlayerCommandInput` adapter over `CMB-REF-004` and `CMB-API-002` | Completed normal right-click + exactly one selected Combat owner + resolved other Combat Root -> atomic Manual Target assignment/Auto Fire disable; rejected targeting -> unchanged existing destination dispatch | Multi-ship broadcast, faction inference, Fire Eligibility mutation, steering/pursuit, formation interception, firing/reload commit, or direct Combat-state writes by input |
| Shot Sample construction | Future Combat fire-resolution/shot-sampling owner | Eligibility and participation -> fire solution and Dispersion -> copied Active muzzle-socket pose -> immutable Shot Sample | Reload/eligibility ownership, required target identity, mutable socket Transform, Projectile state, Hit/Miss, damage, VFX, UI |
| Projectile simulation | Future Projectile owner | Shot Sample -> world-space movement/lifetime/collision query -> neutral Projectile result | Damage/Integrity, Hit/Miss rule authority, ship state, VFX/UI, Movement |
| Hit Query | Future Combat spatial-query owner | Projectile result + Active Combat Geometry filter/resolution -> neutral target/hit context | Integrity mutation, render-mesh inference, VFX decisions |
| Combat Resolution | Future Combat Resolution owner | Hit context + target/rules -> authoritative Hit/Miss and damage decision | Projectile integration, art mutation, Renderer/Mesh/VisualRoot dependency |
| Ship Integrity | Future registered damage-resolution owner | Valid hostile damage result -> Integrity/lifecycle state | Direct Projectile/VFX writes; crew, rigging, fire, flooding, or per-region HP |
| Dispersion | Future Combat fire-resolution/shot-sampling owner | Combat-owned configuration + aim basis -> sampled launch error -> Shot Sample | Exposure, Combat Geometry, rendering, target scoring, Hit/Miss |
| Combat VFX presentation | Active Combat Art/VFX receiver boundary | Resolved immutable Combat event facts -> presentation | Outcome decisions, Projectile/damage/Integrity ownership, Movement writes |

The freeze rejects direct `Projectile -> Ship Integrity`, VFX-owned gameplay
outcomes, target geometry sourced from render data, Dispersion derived from or
stored in Exposure/Combat Geometry, retained writable muzzle Transforms in Shot
Samples, and any Combat or Combat AI write to the Movement Root pose.

The following implementation details remain intentionally open: concrete type and
method names, Shot Sample/result schemas, Projectile integration and collision
mechanics, Hit Query ordering details, damage formula, scoring factors, timing and
dispatch, and all playtest-bound values. Changing any frozen ownership or
dependency direction requires a Registry update before or with implementation.

## Temporary / playtest-bound balance contracts

All values remain temporary and playtest-bound. Phase 1B activates
`CMB-FLD-003`; Phase 2A/2B use Active `CMB-FLD-001` and `CMB-FLD-002`; Phase 3B
also consumes `CMB-FLD-002` for its temporary Range Quality. The other balance
contracts remain `Planned`. No current numeric default or scoring curve is
frozen as final balance.

| ID | Balance contract | Owner | Unit / Space | Meaning | Source of Truth / Writable By | Planned Consumers | Lifecycle | Maturity | Status | Evidence |
|---|---|---|---|---|---|---|---|---|---|---|
| `CMB-FLD-001` | `ShipFireEligibility.forwardArcLimitDegrees` / `aftArcLimitDegrees` and read-only properties | Combat design/gameplay | `deg`; `Ship Root Local Space` horizontal signed-bearing convention | Shared Port/Starboard arc extents measured from either 90-degree broadside direction toward Bow and Stern. Legal absolute bearing is inclusive from `90 - forward` through `90 + aft`; exact Bow/Stern remain dead-zone directions. Each extent is finite and within 0–90 degrees, their sum is positive and cannot exceed the approved 160-degree Foundation upper bound. Current defaults `80` forward / `70` aft are temporary implementation data, not per-gun arcs, final balance, Heading control, or socket direction. | Private serialized fields on `ShipFireEligibility`; Combat prefab/scene authoring may tune them; public getters and queries are read-only; invalid values make `CMB-API-007` / `CMB-API-012` fail and `CMB-API-013` reject without mutation | Known: `CMB-API-007`, `CMB-API-011` through `013`, `CMB-STA-006`, `CMB-STA-020`, `CMB-STA-021`; planned Auto/Manual firing, Basic Combat AI/debug | Static per-component configuration sampled on each query/action | Temporary / Playtest-bound | `Active` | `ShipFireEligibility.cs`; shared geometry; Phase 2 boundaries and Phase 4A/4B query/action tests. |
| `CMB-FLD-002` | `ShipFireEligibility.effectiveRangeMeters` / `maximumRangeMeters` and read-only properties | Combat design/gameplay | `m`; direct `World Space` Root-to-Root or Root-to-Blind-Fire-point distance | Maximum Range is the inclusive hard legality boundary for target-aware and Blind Fire point mode. Blind Fire direction mode has no point distance and no Maximum Range verdict. Effective Range is informational for Fire Eligibility and is not a Blind Fire legality boundary. For Active Phase 3B scoring only, temporary Range Quality is `1` at distance `<= Effective`, decreases linearly to `0` between Effective and Maximum, and is `0` at/after Maximum. Current defaults `300 m` / `500 m` and this linear formula are playtest-bound. Range Quality does not alter Exposure and is not final balance, Projectile lifetime/range, distance-based Exposure shrinking, or accuracy. | Private serialized fields on `ShipFireEligibility`; Combat prefab/scene authoring may tune them; public access is read-only; invalid values make queries fail and `CMB-API-013` reject without mutation | Known: target-aware queries/scoring, `CMB-API-012`, `CMB-API-013`, and `CMB-STA-019` through `021`; planned fire solution | Static per-component configuration sampled on each query/action | Temporary / Playtest-bound | `Active` | `ShipFireEligibility.cs`; `ShipAutoTargetScorer.cs`; Phase 2/3B and Phase 4A/4B range tests. |
| `CMB-FLD-003` | `ShipCombatState.broadsideReloadDurationSeconds` / `BroadsideReloadDurationSeconds` | Combat design/gameplay | `s`; `Scalar / N/A` | One shared reload duration used when either Ready broadside commits. The current `30 s` default is implementation data, not frozen balance; finite values are clamped to a positive `0.01 s` minimum. It is not per-side/per-cannon configuration, elapsed/remaining time, or a public setter. | Private serialized field on `ShipCombatState`; authoring may tune it; public getter is read-only; owner sanitizes invalid values | Known: `CMB-STA-002`, `CMB-API-006`, and successful `CMB-API-013`; planned UI/AI reads | Sampled on each successful commit; rejected attempts and configuration changes do not restart an in-progress timer | Temporary / Playtest-bound | `Active` | `ShipCombatState.cs`; Gelderland serialized value; state and Phase 4B timer-isolation tests. |
| `CMB-FLD-004` | Dispersion curves | Combat design/gameplay | Units and domain TBD; `Shot / Dispersion Plane Space` | Future spread behavior independent of Target Exposure. | Future Combat configuration; design-authorized balance edits | Dispersion and Shot Sample | Static config sampled per shot | Temporary / Playtest-bound | `Planned` | Approved Phase 0.5 / Change 3 category; value evidence: none. |
| `CMB-FLD-005` | Integrity scale | Combat design/gameplay | Unit/scale TBD; `Scalar / N/A` | Future maximum/current Integrity scale. | Future Combat configuration/state; design-authorized balance edits and registered damage writes | Damage/lifecycle, AI, VFX, later HUD | Config plus runtime state | Temporary / Playtest-bound | `Planned` | Approved Phase 0.5 / Change 3 category; value evidence: none. |
| `CMB-FLD-006` | Disabled threshold | Combat design/gameplay | Same future Integrity scale; `Scalar / N/A` | Future boundary for entering Combat Disabled. | Future Combat configuration; design-authorized balance edits | Combat lifecycle, Fire Eligibility, AI/VFX | Static config evaluated on Integrity changes | Temporary / Playtest-bound | `Planned` | Approved Phase 0.5 / Change 3 category; value evidence: none. |
| `CMB-FLD-007` | Sinking rules | Combat design/gameplay | Values/units TBD | Future conditions and timing for Sinking. | Future Combat configuration/rules; architecture/design-authorized edits | Combat lifecycle, AI/VFX, later HUD | Runtime transition rules over future state | Temporary / Playtest-bound | `Planned` | Approved Phase 0.5 / Change 3 category; value evidence: none. |
| `CMB-FLD-008` | Raking multipliers | Combat design/gameplay | `Scalar / N/A` | Future modifiers applied by valid Raking resolution. | Future Combat configuration; design-authorized balance edits | Raking and damage resolution, AI/debug | Static config sampled during resolution | Temporary / Playtest-bound | `Planned` | Approved Phase 0.5 / Change 3 category; value evidence: none. |

## Explicitly deferred systems

The following are outside Foundation scope and have no approved implementation
contract or API in this skeleton:

- Chain Shot
- Grape Shot
- Crew complex interactions
- Rigging damage
- Fire/Flooding
- complex criticals
- per-gun independent arc
- per-gun independent reload
- half broadside
- boarding
- commander duel
- advanced combat AI
- full Combat HUD

Adding any deferred system requires an explicit architecture/Registry change;
its appearance in this list does not reserve an API or implementation design.
