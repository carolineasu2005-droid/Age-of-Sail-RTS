# Combat Art / Ship Spatial Integration Registry

**Registry status:** Foundation / Partially Active

This Registry records approved Combat Art and ship-spatial boundaries. The
minimal `ShipArtDefinition` component, its explicit reference accessors,
Length/Beam queries, the dedicated Phase 1 placeholder-prefab integration, and
the Phase 2A `ShipMuzzleSockets` collection contract, the Phase 2B Gelderland
muzzle hierarchy and passive Scene-view gizmos, the Phase 3A Combat Geometry
semantic/query contract, and the Phase 3B Gelderland query-hull integration
are `Active`. Phase 4A activates the code-only, stateless Target Exposure
Reference contract, and Phase 4B integrates it on the Gelderland Combat Root
with passive Scene-view visualization. Phase 5A activates the code-only Combat
VFX event data and receiver boundary. Phase 5B adds a runtime placeholder
receiver for spatial verification only. Final VFX assets and systems,
formal-art calibration, and the validator remain `Planned` until implementation
evidence is added.

Governing dependencies:

- `PRJ-CONV-001` defines 1 Unity Unit = 1 meter.
- `PRJ-CONV-002` defines +Z Bow, +X Starboard, and +Y Up.
- `PRJ-CONV-003` / `MOV-REF-001` define the Formal Ship Root.
- `MOV-STA-001` and `MOV-STA-002` retain position and Heading authority.
- `PRJ-CONV-004` keeps `VisualRoot` presentation-only.
- `PRJ-CONV-005` defines Combat Geometry layer index `8`, Trigger volumes, and
  explicit `QueryTriggerInteraction.Collide` query behavior.

Combat Art may read the Movement Root and build child-space references from it.
It must not write Movement Root position or Heading.
Movement remains the writer of authoritative Root movement state:
`ShipSailingSpeed` owns Root translation and `ShipTurning` owns Root Heading,
as registered by `MOV-STA-001` and `MOV-STA-002`.

## Planned ship hierarchy

```text
Formal Ship Root
├── VisualRoot
├── CollisionRoot
├── DebugRoot
├── ArtReferences
├── CombatSockets
├── CombatGeometry
└── Exposure
```

This is the approved full target hierarchy. `PF_Ship_Gelderland_Combat_v01`
currently implements the inherited `VisualRoot`, `CollisionRoot`, and
`DebugRoot` plus distinct `ArtReferences`, `CombatSockets`, and
`CombatGeometry` hierarchies. The sibling `Exposure` hierarchy remains Planned;
Phase 4A/4B deliberately use a Root component plus mathematical results and do
not create or activate that optional child hierarchy.

| ID | Contract | Kind | Owner | Purpose / Meaning | Access / Type | Unit | Coordinate Space | Source of Truth | Writable By | Known / Planned Consumers | Lifecycle | Side Effects | Status | Do Not Interpret As | Replacement | Evidence |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `ART-PFB-001` | Combat-ready ship hierarchy | Prefab hierarchy | Combat Art | Establishes the complete named sibling-root target beneath the Formal Ship Root. | Planned prefab hierarchy | `Scalar / N/A` | `Ship Root Local Space` | Future validated combat-ready ship prefab | Combat Art prefab authoring only; never Combat runtime pose logic | Known: none. Planned: Combat Art, Combat Gameplay, validator. | Static prefab/configuration data when implemented | N/A until implemented | `Planned` | The partial Phase 1 placeholder hierarchy, a current-prefab guarantee, or permission to move the Formal Ship Root. | N/A | Approved Phase 0.5 / Change 3 architecture; complete-hierarchy implementation evidence: none. |
| `ART-PFB-004` | `PF_Ship_Gelderland_Combat_v01` | Prefab Variant contract | Combat Art | Dedicated Gelderland Combat placeholder that inherits the complete Medium Movement stack while adding `ShipArtDefinition`, `ShipMuzzleSockets`, `ShipCombatGeometry`, `ShipExposureReference`, and distinct `ArtReferences`, `CombatSockets`, and `CombatGeometry` children on the authoritative Root. Current anchors produce approximately 45 m Length, 11 m Beam, and 2 m gameplay hull Exposure Height; muzzle, hull geometry, and art-reference values are provisional. | Prefab Variant of `PF_Proxy_Medium_v01` at `Assets/Assets/Game/Ship/Proxy/PF_Ship_Gelderland_Combat_v01.prefab` | Position/dimensions `m` | Combat Art children: `Ship Root Local Space`; inherited Root pose and Exposure result: `World Space` | The variant asset plus inherited Medium source prefab; serialized Combat Art references select anchors, muzzle containers, independent Combat hull regions, and the Target Exposure art-definition source | Combat Art prefab authoring may recalibrate variant-local placeholders; Movement alone writes Root pose at runtime | Known: `ShipArtDefinition`, `ShipMuzzleSockets`, `ShipCombatGeometry`, `ShipExposureReference`, Scene-view gizmos, EditMode prefab tests, focused PlayMode integration/physics tests. Planned: Projectile, line of fire, Hit Context, validator. | Static prefab configuration; inherited children and authored Combat Art references follow Root pose at runtime | Trigger volumes participate only in explicit filtered physics queries; Exposure is a stateless read calculation; Root Movement behavior is inherited unchanged | `Active` | Final Gelderland art/dimensions/socket/hull calibration, a new Movement Root, runtime target state, or the generic source used to rebuild Light/Heavy proxies. | N/A | Prefab variant GUID `45f76b674dece124096d84bb473f8322`; base prefab GUID `f767a1e80c8cd1843a0e947ca3c36def`; Combat Art, muzzle-socket, Combat Geometry, and Target Exposure prefab/integration tests. |

## ShipArtDefinition

| ID | Contract | Kind | Owner | Purpose / Meaning | Access / Type | Unit / Space | Source of Truth | Writable By | Known / Planned Consumers | Lifecycle | Status | Do Not Interpret As | Replacement | Evidence |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `ART-SO-001` | `ShipArtDefinition` data asset concept | ScriptableObject data | Combat Art | Historical planned asset shape superseded before implementation by the root-local component contract `ART-API-001`. | No implemented API | `Scalar / N/A` | Registry history | Nobody | None | Never implemented | `Removed` | An available asset type or an alternate source of spatial truth. | `ART-API-001` | Superseded by Combat Art Phase 1 / Change 1A. |
| `ART-API-001` | `ShipArtDefinition` | `MonoBehaviour` component and read contract | Combat Art | Passive root-local holder for explicit ship-art references. It derives spatial dimensions without owning or mutating Movement pose. | Addable to the authoritative Ship Root; eight public getter-only `Transform` accessors; no update loop | References: `Scalar / N/A`; derived positions/dimensions use `Ship Root Local Space` and `m` | Private serialized fields on the component; the component's own `transform` is the spatial root | Combat Art prefab/scene authoring assigns fields; runtime consumers are read-only | Known: `PF_Ship_Gelderland_Combat_v01` and EditMode/PlayMode contract tests. Planned: Combat Gameplay spatial reads and validator. | Static configuration with on-demand read queries; reads have no hierarchy or Movement side effects | `Active` | A Movement Root replacement, runtime movement controller, automatic hierarchy builder, or final formal-art calibration. | N/A | `ShipArtDefinition.cs`; component on the variant Root; Combat Art contract, prefab, and integration tests. |
| `ART-API-002` | `TryGetLength(out float length)` | Query API | Combat Art | Derives Length as the absolute difference between Bow and Stern reference positions on the root-local Z axis. Returns unavailable with output `0` when either reference is missing. | `public bool TryGetLength(out float length)` | `m`; `Ship Root Local Space`, +Z Bow | `BowReference` and `SternReference`, converted with the component root's `InverseTransformPoint` | Derived read-only value; nobody writes it | Known: Combat placeholder variant, `ShipExposureReference`, and EditMode/focused PlayMode tests. Planned: other Combat Gameplay spatial reads and validator/debug visualization. | On-demand query; no side effects | `Active` | Renderer, mesh, collider, world-axis, or final Gelderland length data. | N/A | `ShipArtDefinition.TryGetLength`; contract, prefab, root-motion, and Target Exposure tests. |
| `ART-API-003` | `TryGetBeam(out float beam)` | Query API | Combat Art | Derives Beam as the absolute difference between Starboard and Port reference positions on the root-local X axis. Returns unavailable with output `0` when either reference is missing. | `public bool TryGetBeam(out float beam)` | `m`; `Ship Root Local Space`, +X Starboard | `PortReference` and `StarboardReference`, converted with the component root's `InverseTransformPoint` | Derived read-only value; nobody writes it | Known: Combat placeholder variant, `ShipExposureReference`, and EditMode/focused PlayMode tests. Planned: other Combat Gameplay spatial reads and validator/debug visualization. | On-demand query; no side effects | `Active` | Renderer, mesh, collider, world-axis, or final Gelderland beam data. | N/A | `ShipArtDefinition.TryGetBeam`; contract, prefab, root-motion, and Target Exposure tests. |

## ArtReferences

`ArtReferences` is the formal cross-system spatial-anchor hierarchy on the
Combat placeholder variant. It is separate from inherited `DebugRoot`, which
remains historical Movement/debug support. Consumers must use the assigned
`ShipArtDefinition` references rather than search render children, infer
geometry from a mesh, or consume DebugRoot points as Combat contracts.

| ID | Contract | Kind | Owner | Purpose / Meaning | Unit | Coordinate Space | Source of Truth | Writable By | Known / Planned Consumers | Lifecycle | Status | Do Not Interpret As | Evidence |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `ART-REF-001` | `ArtReferences/{Waterline,Center,Bow,Stern,Port,Starboard,Deck}` | Reference-container hierarchy contract | Combat Art | Direct child of the authoritative Root organizing seven ordinary Transform anchors. It is semantically separate from `DebugRoot`. | `Scalar / N/A` | `Ship Root Local Space` | `PF_Ship_Gelderland_Combat_v01` variant-local hierarchy | Combat Art prefab authoring; runtime consumers do not replace or move anchors | Known: `ShipArtDefinition` and Combat Art prefab tests. Planned: Combat Gameplay and validator. | Static prefab configuration; anchors inherit Root movement/rotation | `Active` | A Movement debug hierarchy, runtime Combat state, automatic builder output, or final formal-art calibration. | Combat placeholder prefab and structure tests. |
| `ART-REF-002` | `VisualRoot` reference | Transform reference | Combat Art | Points to the inherited presentation/render hierarchy defined by `PRJ-CONV-004`; assignment is optional at the API level and assigned on the Combat placeholder. | `Scalar / N/A` | `Ship Root Local Space` | `ShipArtDefinition.visualRoot`; public read via `VisualRoot` | Combat Art prefab/scene authoring; no public runtime setter | Known: `PF_Ship_Gelderland_Combat_v01` and EditMode contract/prefab tests. Planned: Combat Art presentation and validator. | Static serialized reference; reading has no side effects | `Active` | Gameplay pose, Combat geometry, or a source for Root Heading. | Variant assignment to inherited `VisualRoot`; Combat Art prefab tests. |
| `ART-REF-003` | `WaterlineReference` | Transform reference | Combat Art | Represents the intended ship waterline reference plane/height; only its spatial anchor is implemented and the exact assigned transform awaits calibration. | Position `m` | `Ship Root Local Space` | `ShipArtDefinition.waterlineReference`; public read via `WaterlineReference` | Combat Art prefab/scene authoring; no public runtime setter | Known: Combat placeholder variant and EditMode contract/prefab tests. Planned: Combat Art/VFX and validator. | Static serialized reference; reading has no side effects | `Active` | Buoyancy, sinking logic, final waterline height, or Movement Root position. | Variant assignment to `ArtReferences/Waterline`; contract and prefab tests. |
| `ART-REF-004` | `CenterReference` | Transform reference | Combat Art | Stable semantic center of the gameplay-relevant ship body/art reference for use as a common spatial anchor. | Position `m` | `Ship Root Local Space` | `ShipArtDefinition.centerReference`; public read via `CenterReference` | Combat Art prefab/scene authoring; no public runtime setter | Known: Combat placeholder variant and EditMode contract/prefab tests. Planned: Combat Art, Combat Gameplay, and validator. | Static serialized reference; reading has no side effects | `Active` | `Renderer.bounds.center`, center of mass, or collider center. | Variant assignment to `ArtReferences/Center`; contract and prefab tests. |
| `ART-REF-005` | `BowReference` | Transform reference | Combat Art | Explicit bow reference aligned to the +Z ship convention and used with Stern to derive Length. | Position `m` | `Ship Root Local Space` | `ShipArtDefinition.bowReference`; public read via `BowReference` | Combat Art prefab/scene authoring; no public runtime setter | Known: Combat placeholder variant, Length query, and EditMode/PlayMode tests. Planned: Combat Gameplay and validator. | Static serialized reference; reading has no side effects | `Active` | Final bow dimension or muzzle socket. | Variant assignment to `ArtReferences/Bow`; query, prefab, and integration tests. |
| `ART-REF-006` | `SternReference` | Transform reference | Combat Art | Explicit stern reference opposite the +Z bow direction and used with Bow to derive Length. | Position `m` | `Ship Root Local Space` | `ShipArtDefinition.sternReference`; public read via `SternReference` | Combat Art prefab/scene authoring; no public runtime setter | Known: Combat placeholder variant, Length query, and EditMode/PlayMode tests. Planned: Combat Gameplay and validator. | Static serialized reference; reading has no side effects | `Active` | Final stern dimension or propulsion implementation. | Variant assignment to `ArtReferences/Stern`; query, prefab, and integration tests. |
| `ART-REF-007` | `PortReference` | Transform reference | Combat Art | Explicit port-side reference opposite the +X Starboard axis and used with Starboard to derive Beam. | Position `m` | `Ship Root Local Space` | `ShipArtDefinition.portReference`; public read via `PortReference` | Combat Art prefab/scene authoring; no public runtime setter | Known: Combat placeholder variant, Beam query, and EditMode/PlayMode tests. Planned: Combat Gameplay and validator. | Static serialized reference; reading has no side effects | `Active` | A Port muzzle collection or broadside arc definition. | Variant assignment to `ArtReferences/Port`; query, prefab, and integration tests. |
| `ART-REF-008` | `StarboardReference` | Transform reference | Combat Art | Explicit starboard-side reference aligned with +X and used with Port to derive Beam. | Position `m` | `Ship Root Local Space` | `ShipArtDefinition.starboardReference`; public read via `StarboardReference` | Combat Art prefab/scene authoring; no public runtime setter | Known: Combat placeholder variant, Beam query, and EditMode/PlayMode tests. Planned: Combat Gameplay and validator. | Static serialized reference; reading has no side effects | `Active` | A Starboard muzzle collection or broadside arc definition. | Variant assignment to `ArtReferences/Starboard`; query, prefab, and integration tests. |
| `ART-REF-009` | `DeckReference` | Transform reference | Combat Art | Primary deck-height semantic anchor; exact assigned transform awaits formal-art calibration. | Position `m` | `Ship Root Local Space` | `ShipArtDefinition.deckReference`; public read via `DeckReference` | Combat Art prefab/scene authoring; no public runtime setter | Known: Combat placeholder variant, `ShipExposureReference`, and EditMode contract/prefab/exposure tests. Planned: other Combat Art, Combat Gameplay, and validator consumers. | Static serialized reference; reading has no side effects | `Active` | Crew, Boarding, Deck VFX, a collision surface, mast/sail height, or final formal-art calibration. | Variant assignment to `ArtReferences/Deck`; contract, prefab, and Target Exposure tests. |
| `ART-CONV-005` | `DebugRoot` / `ArtReferences` separation | Convention | Combat Art / Movement boundary | `DebugRoot` remains historical Movement/debug support. Combat Art consumers use the distinct `ArtReferences` anchors wired through `ShipArtDefinition`; coincident placeholder positions do not merge their identities or ownership. | `Scalar / N/A` | `Ship Root Local Space` | This Registry contract and the distinct Transform objects in `PF_Ship_Gelderland_Combat_v01` | Architecture changes only; Combat Art may recalibrate ArtReferences without redefining DebugRoot | Known: Combat placeholder prefab and structure tests. Planned: Combat Gameplay and validator. | Active throughout placeholder and formal-art integration | `Active` | Permission to bind Combat contracts permanently to Movement debug points. | Distinct `ArtReferences` and `DebugRoot` prefab hierarchies; identity assertions in EditMode tests. |

## Muzzle Sockets

```text
CombatSockets
└── Muzzles
    ├── Port
    └── Starboard
```

`ShipMuzzleSockets` is the Active code contract for collecting and reading
authored muzzle sockets. Phase 2B adds the hierarchy to the Gelderland Combat
placeholder with one provisional main-deck row of 13 Port and 13 Starboard
sockets. Count, side, canonical ordering, and direction are Active contracts;
the placeholder transforms remain recalibratable when formal art arrives.

| ID | Contract | Kind | Owner | Purpose / Meaning | Access / Type | Unit | Coordinate Space | Source of Truth | Writable By | Known / Planned Consumers | Lifecycle | Side Effects | Status | Do Not Interpret As | Replacement | Evidence |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `ART-API-004` | `ShipMuzzleSockets` | `MonoBehaviour` component and collection contract | Combat Art | Passive root-local collector exposing the two authored broadside socket collections without owning firing or Movement. | Addable to the authoritative Ship Root; two getter-only `IReadOnlyList<Transform>` properties; no update loop | References: `Scalar / N/A`; positions `m`; orientation `deg` | Ordering in `Ship Root Local Space`; socket `Transform.forward` is a World-space direction derived from authored local rotation | Serialized Port/Starboard container references and their direct child socket Transforms; cached lists are derived | Combat Art prefab/scene authoring assigns containers and authors sockets; runtime consumers are read-only | Known: Gelderland Combat variant, Scene-view gizmos, and EditMode/PlayMode tests. Planned: Combat Gameplay, Shot Sample, Projectile, VFX, and validator. | Cache rebuilt in `Awake`, in Editor `OnValidate`, or on first read when uninitialized/container references changed; no per-frame scan | Collection reads may initialize the cache; never creates/moves Transforms or writes Movement state | `Active` | A firing, reload, targeting, projectile, VFX, validation, or automatic hierarchy-building system | N/A | `ShipMuzzleSockets.cs`; Gelderland Combat variant; contract, prefab, and root-rotation tests. |
| `ART-API-005` | `ShipMuzzleSockets.PortMuzzles` | Read-only collection API | Combat Art | Returns direct children of the authored Port container in canonical Bow-to-Stern order; missing container returns an empty collection. | `public IReadOnlyList<Transform> PortMuzzles` with no setter; read-only wrapper over a private derived list | Socket positions `m` | Ordered by authoritative `Ship Root Local Space` Z | `ART-REF-011` container children and Root-local positions | Combat Art authoring changes source Transforms; callers cannot mutate cached order through the returned collection | Known: EditMode contract tests. Planned: Combat Gameplay, Shot Sample, Projectile, VFX, and validator. | Stable cached collection after initialization; refreshed by the component lifecycle in `ART-API-004` | May initialize cache; no hierarchy or Movement side effects | `Active` | A mutable list, cannon meshes, firing sequence, broadside arc, or proof that Phase 2B sockets exist | N/A | `ShipMuzzleSockets.PortMuzzles`; collection, ordering, invariance, missing-data, and immutability tests. |
| `ART-API-006` | `ShipMuzzleSockets.StarboardMuzzles` | Read-only collection API | Combat Art | Returns direct children of the authored Starboard container in canonical Bow-to-Stern order; missing container returns an empty collection. | `public IReadOnlyList<Transform> StarboardMuzzles` with no setter; read-only wrapper over a private derived list | Socket positions `m` | Ordered by authoritative `Ship Root Local Space` Z | `ART-REF-012` container children and Root-local positions | Combat Art authoring changes source Transforms; callers cannot mutate cached order through the returned collection | Known: EditMode contract tests. Planned: Combat Gameplay, Shot Sample, Projectile, VFX, and validator. | Stable cached collection after initialization; refreshed by the component lifecycle in `ART-API-004` | May initialize cache; no hierarchy or Movement side effects | `Active` | A mutable list, cannon meshes, firing sequence, broadside arc, or proof that Phase 2B sockets exist | N/A | `ShipMuzzleSockets.StarboardMuzzles`; collection, ordering, invariance, missing-data, and immutability tests. |
| `ART-REF-011` | `ShipMuzzleSockets.portMuzzlesContainer` | Serialized Transform reference | Combat Art | Explicit authored boundary containing Port muzzle sockets as direct children; avoids whole-ship hierarchy or mesh scanning. | Private `[SerializeField] Transform`; consumed through `ART-API-005` | `Scalar / N/A` for the reference; child positions `m` | `CombatSockets/Muzzles/Port` beneath the authoritative Root; ordering resolved in `Ship Root Local Space` | Serialized component field and assigned authored hierarchy | Combat Art prefab/scene authoring; no public runtime setter | Known: `PF_Ship_Gelderland_Combat_v01`, `ShipMuzzleSockets`, gizmos, and EditMode/PlayMode tests. Planned: validator and Combat Gameplay. | Static configuration sampled by the cache lifecycle in `ART-API-004` | Assignment alone has no runtime side effect; missing reference yields an empty Port collection | `Active` | A cannon model collection, firing state, or Combat-owned mutable state | N/A | Gelderland Combat variant assignment; `ShipMuzzleSockets.portMuzzlesContainer`; missing-container, collection, prefab, and rotation tests. |
| `ART-REF-012` | `ShipMuzzleSockets.starboardMuzzlesContainer` | Serialized Transform reference | Combat Art | Explicit authored boundary containing Starboard muzzle sockets as direct children; avoids whole-ship hierarchy or mesh scanning. | Private `[SerializeField] Transform`; consumed through `ART-API-006` | `Scalar / N/A` for the reference; child positions `m` | `CombatSockets/Muzzles/Starboard` beneath the authoritative Root; ordering resolved in `Ship Root Local Space` | Serialized component field and assigned authored hierarchy | Combat Art prefab/scene authoring; no public runtime setter | Known: `PF_Ship_Gelderland_Combat_v01`, `ShipMuzzleSockets`, gizmos, and EditMode/PlayMode tests. Planned: validator and Combat Gameplay. | Static configuration sampled by the cache lifecycle in `ART-API-004` | Assignment alone has no runtime side effect; missing reference yields an empty Starboard collection | `Active` | A cannon model collection, firing state, or Combat-owned mutable state | N/A | Gelderland Combat variant assignment; `ShipMuzzleSockets.starboardMuzzlesContainer`; missing-container, collection, prefab, and rotation tests. |
| `ART-CONV-006` | Canonical muzzle order: Bow to Stern | Ordering convention | Combat Art | Both side collections are sorted by descending authoritative Root-local Z: larger Z is earlier/more forward. Root-local Z differences within 0.0001 m use ascending sibling index only as a deterministic tie-break. | Applied by `ShipMuzzleSockets` cache construction | Position `m` | `Ship Root Local Space` | Socket Transform positions relative to the component's authoritative Root | Combat Art authors socket positions; `ShipMuzzleSockets` derives but does not mutate order | Known: Gelderland Combat variant, `ShipMuzzleSockets`, gizmos, and EditMode/PlayMode tests. Planned: Combat Gameplay, Shot Sample, Projectile, VFX, and validator. | Applied whenever the cache rebuilds | Determines read order only; does not prescribe firing animation/order or alter hierarchy sibling order | `Active` | World-Z sorting, name sorting, primary sibling-order sorting, or a required firing sequence | N/A | `ShipMuzzleSockets.CompareCanonicalOrder`; Gelderland sockets; spatial, sibling-order, transformed-container, Root-invariance, prefab, and PlayMode tests. |
| `ART-CONV-007` | Muzzle nominal direction | Socket orientation convention | Combat Art | Every muzzle socket's local +Z / `Transform.forward` is its nominal physical firing direction. Port is authored approximately toward Root-local -X; Starboard approximately toward Root-local +X. | Unity Transform orientation | Direction `Scalar / N/A`; authored rotation `deg` | Socket Local Space converted through the ship hierarchy to `World Space` | Each authored muzzle socket Transform rotation | Combat Art prefab/model authoring; runtime consumers read only | Known: Gelderland Combat variant, Scene-view gizmos, and EditMode/PlayMode tests. Planned: Combat Gameplay, Shot Sample, Projectile, VFX, and validator. | Static authored orientation following parent/Root transforms at runtime | Defines nominal origin direction only; does not rotate the Root, steer the ship, encode Broadside Arc, or fire a projectile | `Active` | Broadside eligibility, aim solution, dispersion, cannon mesh pivot, or Movement Heading authority | N/A | Unity Transform semantics; Gelderland orientation and root-rotation tests. |

| ID | Contract | Kind | Owner | Purpose / Meaning | Access / Type | Unit | Coordinate Space | Source of Truth | Writable By | Known / Planned Consumers | Lifecycle | Side Effects | Status | Do Not Interpret As | Replacement | Evidence |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `ART-PFB-002` | `CombatSockets/Muzzles` hierarchy | Prefab hierarchy | Combat Art | Separates Combat socket organization from render and collision hierarchies. The Gelderland Combat variant implements `CombatSockets/Muzzles/{Port,Starboard}` as identity-organizing transforms containing exactly 26 main-battery sockets. | Authored Transform hierarchy beneath the authoritative Root | `Scalar / N/A` | `Ship Root Local Space` | `PF_Ship_Gelderland_Combat_v01` variant hierarchy | Combat Art prefab authoring | Known: `ShipMuzzleSockets`, Scene-view gizmos, prefab/integration tests. Planned: Combat Gameplay and validator. | Static prefab/configuration data | Child sockets follow Root pose; hierarchy itself performs no runtime behavior | `Active` | A cannon inventory, reload owner, automatic hierarchy builder, or a guarantee that every future ship has the Gelderland count. | N/A | Gelderland Combat variant hierarchy and prefab structure tests. |
| `ART-SCK-001` | Port muzzle sockets | Socket collection | Combat Art | Exactly 13 provisional main-battery emission Transforms for the first Gelderland Combat ship, named `P01` through `P13` and resolved in canonical Bow-to-Stern spatial order. | Direct child `Transform` sockets read through `ART-API-005` | Position `m`; orientation `deg` | `Ship Root Local Space`; each socket local +Z points approximately Root-local -X | Authored Muzzle Socket Transforms beneath `CombatSockets/Muzzles/Port`; not mesh/render/debug data | Combat Art prefab/art integration authoring; future runtime Combat is read-only | Known: `ShipMuzzleSockets`, Scene-view gizmos, prefab/integration tests. Planned: Combat Gameplay shot spawning, Combat VFX, validator. | Static prefab/configuration data; follows Root pose | Reading has no pose side effect; Root movement naturally changes derived World position/direction | `Active` | Port broadside state, firing sequence, a per-gun reload design, cannon-tip nesting, or final Gelderland socket transforms. | N/A | Frozen Phase 2 Gelderland count/order RPD; Gelderland Combat variant and muzzle-socket tests. |
| `ART-SCK-002` | Starboard muzzle sockets | Socket collection | Combat Art | Exactly 13 provisional main-battery emission Transforms for the first Gelderland Combat ship, named `S01` through `S13` and resolved in canonical Bow-to-Stern spatial order. | Direct child `Transform` sockets read through `ART-API-006` | Position `m`; orientation `deg` | `Ship Root Local Space`; each socket local +Z points approximately Root-local +X | Authored Muzzle Socket Transforms beneath `CombatSockets/Muzzles/Starboard`; not mesh/render/debug data | Combat Art prefab/art integration authoring; future runtime Combat is read-only | Known: `ShipMuzzleSockets`, Scene-view gizmos, prefab/integration tests. Planned: Combat Gameplay shot spawning, Combat VFX, validator. | Static prefab/configuration data; follows Root pose | Reading has no pose side effect; Root movement naturally changes derived World position/direction | `Active` | Starboard broadside state, firing sequence, a per-gun reload design, cannon-tip nesting, or final Gelderland socket transforms. | N/A | Frozen Phase 2 Gelderland count/order RPD; Gelderland Combat variant and muzzle-socket tests. |

## Combat Geometry

Phase 3B integrates the Scheme B semantic contract on the Gelderland Combat
placeholder:

```text
CombatGeometry
└── MainHull                 (logical grouping; no collider required)
    ├── Bow                  (actual query region)
    ├── Midship              (actual query region)
    └── Stern                (actual query region)
```

The three region objects carry Trigger BoxColliders on the dedicated
`CombatGeometry` Unity Layer at index `8`. `MainHull` remains a collider-free
logical container. Current centers and sizes are provisional placeholder
calibration and are not formal Gelderland art data.

| ID | Symbol / Contract | Kind | Owner | Purpose / Meaning | Access / Type | Unit | Coordinate Space | Source of Truth | Writable By | Known Consumers | Planned Consumers | Lifecycle | Side Effects | Status | Do Not Interpret As | Replacement | Evidence |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `ART-PFB-003` | `CombatGeometry/MainHull/{Bow,Midship,Stern}` hierarchy | Prefab hierarchy | Combat Art / spatial integration | Dedicated Scheme B hierarchy in which Bow, Midship, and Stern are the three actual coarse hull query volumes beneath a logical, collider-free `MainHull` group. | Authored Transform hierarchy; each region has one `CombatHitRegion` and one Trigger `BoxCollider` | Region dimensions and positions `m` | `Ship Root Local Space`; collider queries operate in `World Space` | Authored Combat Geometry Collider volumes and semantic components on `PF_Ship_Gelderland_Combat_v01` | Combat Art prefab authoring only; runtime consumers are read-only | `ShipCombatGeometry`, Combat Geometry gizmos, EditMode prefab tests, PlayMode physics tests | Projectile, line-of-fire queries, Combat Debug, Damage/Hit Context, validator | Static prefab/configuration data; follows authoritative Root pose at runtime | Enables explicit filtered Trigger queries; does not create physical collision forces or modify Movement | `Active` | Render Mesh, existing generic `CollisionRoot/ShipCollider`, a fourth MainHull collider, or final formal-art calibration. | N/A | `PF_Ship_Gelderland_Combat_v01` hierarchy/component wiring; `GelderlandCombatGeometryPrefabTests`; `GelderlandCombatGeometryPlayModeTests`. |
| `ART-CONV-001` | Combat Geometry separation | Convention | Combat Art / Combat Gameplay boundary | Combat Geometry is query-only gameplay hit geometry and is neither Render Mesh nor the existing generic `CollisionRoot/ShipCollider`. | Foundation architecture and implementation boundary | `Scalar / N/A` | `Scalar / N/A`; concrete geometry uses explicitly registered spaces | This Registry contract plus explicit `ShipCombatGeometry` / `CombatHitRegion` references | Architecture changes only; Combat Art authors dedicated geometry; runtime consumers read/query | `ShipCombatGeometryTests` | Combat Art authoring, Projectile, line-of-fire queries, Combat Debug, validator | Foundation invariant | Prevents render or generic collision changes from silently redefining Combat hits | `Active` | Permission to reuse render triangles, infer from `Renderer.bounds`, or treat generic `ShipCollider` as Combat hit truth. | N/A | `ShipCombatGeometry.cs`; `CombatHitRegion.cs`; no-renderer, no-Movement, unrelated-collider, and logical-MainHull EditMode tests. |
| `ART-API-007` | `ShipCombatGeometry` | `MonoBehaviour` component and read contract | Combat Art | Passive owner of the authored MainHull grouping and Bow/Midship/Stern semantic region references. | Addable component; four public getter-only properties; no update loop | References `Scalar / N/A`; authored geometry dimensions/positions `m` | Authored hierarchy in `Ship Root Local Space`; referenced colliders participate in `World Space` queries | Private serialized `mainHullRoot`, `bowRegion`, `midshipRegion`, and `sternRegion` fields | Combat Art prefab/scene authoring assigns references; runtime callers are read-only | `ShipCombatGeometryTests` | Projectile, line-of-fire query, Combat Debug, Damage/Hit Context, validator | Static authored configuration queried on demand | Reads and queries do not move objects, change colliders, or mutate Movement | `Active` | A collider generator, hierarchy builder, damage owner, Movement controller, or generic `ShipCollider` adapter. | N/A | `ShipCombatGeometry.cs`; EditMode contract, missing-data, dependency-boundary, and read-only-reference tests. |
| `ART-API-008` | `ShipCombatGeometry.TryResolveRegion(Collider, out CombatHitRegion)` | Query API | Combat Art | Resolves an exact configured query Collider to its semantic component only when its assigned owner and Bow/Midship/Stern identity also match; null, unrelated, missing, or inconsistent configuration fails with `false` and null output. | `public bool TryResolveRegion(Collider collider, out CombatHitRegion region)` | `Scalar / N/A` | Collider identity from a `World Space` physics query; no coordinate conversion or positional heuristic | Explicit region references on `ShipCombatGeometry` and the candidate's `Owner`, `Region`, and `QueryCollider` contracts | Derived read-only result; nobody writes through the API | `ShipCombatGeometryTests` | Projectile hit resolution, line-of-fire query, Combat Debug, Damage/Hit Context | On-demand query; valid while authored Unity object references remain valid | No scans, name parsing, hierarchy search, physics mutation, or Movement side effects | `Active` | Damage resolution, hit probability, nearest-region inference, name-based lookup, or permission to accept arbitrary ship colliders. | N/A | `ShipCombatGeometry.TryResolveRegion`; configured-region, unrelated/null, missing-region, ownership, rename, and no-fourth-collider EditMode tests. |
| `ART-API-009` | `CombatHitRegion` | `MonoBehaviour` semantic data component | Combat Art | Identifies one authored coarse hull query volume by semantic region, owning ship geometry, and exact associated Collider. | Addable component; getter-only `Region`, `Owner`, and `QueryCollider` properties; no update loop | Reference/identity `Scalar / N/A`; collider geometry `m` | Authored beneath the ship in `Ship Root Local Space`; collider pose/query in `World Space` | Private serialized `region`, `owner`, and `queryCollider` fields | Combat Art prefab/scene authoring; runtime callers are read-only | `ShipCombatGeometry`, `ShipCombatGeometryTests` | Projectile, line-of-fire query, Combat Debug, Damage/Hit Context, validator | Static authored configuration queried on demand | Reading has no side effects | `Active` | An HP pool, armor/penetration zone, module, critical-hit system, damage rule, or Rigidbody propulsion component. | N/A | `CombatHitRegion.cs`; EditMode identity, ownership, resolution, dependency, and read-only-reference tests. |
| `ART-API-010` | `CombatHullRegion` | Public semantic enum | Combat Art | Minimal stable coarse hull-location identity with exactly `Bow`, `Midship`, and `Stern`. | Public enum | `Scalar / N/A` | `Scalar / N/A` | Authored `CombatHitRegion.region` value | Combat Art prefab/scene authoring; runtime consumers read only | `CombatHitRegion`, `ShipCombatGeometry`, `ShipCombatGeometryTests` | Projectile hit context, Combat Debug, Damage/Hit Context | Static type contract; value persists with its authored region component | Identity only; causes no gameplay behavior | `Active` | Independent health, armor, penetration, mast/sail/gun/rudder/deck/magazine/crew classifications, or an `Unknown` fallback. | N/A | `CombatHullRegion` in `CombatHitRegion.cs`; three-region identity and resolution EditMode tests. |
| `ART-REF-013` | `ShipCombatGeometry.MainHullRoot` / `mainHullRoot` | Serialized Transform reference and read API | Combat Art | Reference to `CombatGeometry/MainHull`, the logical container grouping the three real hull regions. The semantic resolver does not require a Collider on it. | Private `[SerializeField] Transform`; public getter-only `Transform MainHullRoot` | Position `m` | `Ship Root Local Space` | Serialized component field assigned to the Gelderland `MainHull` Transform | Combat Art prefab/scene authoring; no public runtime setter | `PF_Ship_Gelderland_Combat_v01`, `ShipCombatGeometry`, gizmos, Combat Geometry tests | Combat Debug, validator | Static authored reference; follows Root pose; reading has no side effects | None | `Active` | A fourth overlapping hit volume, a required collider, the existing `ShipCollider`, or a source of region identity. | N/A | Gelderland prefab assignment; logical-grouping/no-collider EditMode and prefab tests. |
| `ART-REF-014` | `ShipCombatGeometry.BowRegion` / `bowRegion` | Serialized semantic-region reference | Combat Art | Explicit Bow region reference. Current provisional local center is `(0, 0, 15)` m with a `(9, 4, 15)` m Trigger BoxCollider. | Private `[SerializeField] CombatHitRegion`; public getter-only `CombatHitRegion BowRegion` | Region center/size `m` | `Ship Root Local Space`; collider query in `World Space` | Authored Gelderland Bow `CombatHitRegion` and BoxCollider | Combat Art prefab/scene authoring; no public runtime setter | `PF_Ship_Gelderland_Combat_v01`, `ShipCombatGeometry`, gizmos, Combat Geometry tests | Projectile, line-of-fire query, Combat Debug, Damage/Hit Context, validator | Static provisional prefab calibration; follows Root pose; missing/inconsistent configuration fails safely | None | `Active` | A Bow HP pool, name/hierarchy inference, final formal-art volume, or inclusion of mast/sails/bowsprit. | N/A | Gelderland Bow hierarchy/component/collider assignment; prefab and Bow/root-transform PlayMode query tests. |
| `ART-REF-015` | `ShipCombatGeometry.MidshipRegion` / `midshipRegion` | Serialized semantic-region reference | Combat Art | Explicit Midship region reference. Current provisional local center is `(0, 0, 0)` m with an `(11, 4, 15)` m Trigger BoxCollider. | Private `[SerializeField] CombatHitRegion`; public getter-only `CombatHitRegion MidshipRegion` | Region center/size `m` | `Ship Root Local Space`; collider query in `World Space` | Authored Gelderland Midship `CombatHitRegion` and BoxCollider | Combat Art prefab/scene authoring; no public runtime setter | `PF_Ship_Gelderland_Combat_v01`, `ShipCombatGeometry`, gizmos, Combat Geometry tests | Projectile, line-of-fire query, Combat Debug, Damage/Hit Context, validator | Static provisional prefab calibration; follows Root pose; missing/inconsistent configuration fails safely | None | `Active` | A Midship HP pool, name/hierarchy inference, final formal-art volume, or inclusion of mast/sails/rigging. | N/A | Gelderland Midship hierarchy/component/collider assignment; prefab, renderer-isolation, and trigger-policy PlayMode tests. |
| `ART-REF-016` | `ShipCombatGeometry.SternRegion` / `sternRegion` | Serialized semantic-region reference | Combat Art | Explicit Stern region reference. Current provisional local center is `(0, 0, -15)` m with a `(9, 4, 15)` m Trigger BoxCollider. | Private `[SerializeField] CombatHitRegion`; public getter-only `CombatHitRegion SternRegion` | Region center/size `m` | `Ship Root Local Space`; collider query in `World Space` | Authored Gelderland Stern `CombatHitRegion` and BoxCollider | Combat Art prefab/scene authoring; no public runtime setter | `PF_Ship_Gelderland_Combat_v01`, `ShipCombatGeometry`, gizmos, Combat Geometry tests | Projectile, line-of-fire query, Combat Debug, Damage/Hit Context, validator | Static provisional prefab calibration; follows Root pose; missing/inconsistent configuration fails safely | None | `Active` | A Stern HP pool, name/hierarchy inference, final formal-art volume, or inclusion of decorative lanterns/rigging. | N/A | Gelderland Stern hierarchy/component/collider assignment; prefab, ShipCollider-isolation, and Stern/root-transform PlayMode query tests. |
| `ART-REF-017` | `CombatHitRegion.Owner` / `owner` | Serialized owner reference | Combat Art | Explicitly binds a region to its one owning `ShipCombatGeometry`; the resolver rejects a region assigned to another owner. | Private `[SerializeField] ShipCombatGeometry`; public getter-only `ShipCombatGeometry Owner` | `Scalar / N/A` | `Scalar / N/A`; assigned within the same authored ship hierarchy | Serialized component field assigned on each Gelderland region | Combat Art prefab/scene authoring; no public runtime setter | Gelderland Bow/Midship/Stern regions, `ShipCombatGeometry`, prefab/physics tests | Projectile hit context, Combat Debug, validator | Static authored reference; validated on each resolution query | None | `Active` | A global lookup, parent/name inference, damage ownership, or permission to write ship state. | N/A | Gelderland region owner assignments; `CombatHitRegion.owner` / `Owner`; ownership, wiring, and physics-resolution tests; `ShipCombatGeometry.IsConfiguredMatch`. |
| `ART-REF-018` | `CombatHitRegion.QueryCollider` / `queryCollider` | Serialized Collider reference | Combat Art | Exact query-only Trigger Collider associated with one semantic hull region and placed on `CombatGeometry` layer index `8`. | Private `[SerializeField] Collider`; public getter-only `Collider QueryCollider` | Collider geometry `m` | Collider authored under `Ship Root Local Space`; physics query identity/pose in `World Space` | Authored Bow/Midship/Stern BoxCollider volumes on the Gelderland Combat prefab | Combat Art prefab/scene authoring configures it; runtime consumers and resolver are read-only | `PF_Ship_Gelderland_Combat_v01`, `ShipCombatGeometry`, gizmos, prefab/physics tests | Projectile, line-of-fire query, Combat Debug, Damage/Hit Context, validator | Static authored reference; valid while the Collider exists; follows Root pose | Provides filtered Trigger-query hits only; no collision-force or Movement effects | `Active` | A physical propulsion collider, Rigidbody force source, generic `ShipCollider`, Render Mesh, damage result, or reliance on the global trigger-query setting. | N/A | Gelderland Trigger BoxColliders and component wiring; configured/unrelated/null, layer, Trigger, renderer/ShipCollider-isolation, and transform PlayMode tests. |
| `ART-CONV-008` | Scheme B MainHull and region semantics | Convention | Combat Art / Combat Gameplay boundary | `MainHull` is logical grouping only and does not add a fourth overlapping collider. Bow/Midship/Stern answer only which coarse hull location was hit. | Foundation architecture, runtime contract, and Gelderland prefab hierarchy | Region dimensions/positions `m` | `Ship Root Local Space`; hit query identity in `World Space` | Authored Bow/Midship/Stern Combat Geometry Collider volumes and semantic references | Architecture changes only; Combat Art authors the three region volumes | Gelderland Combat prefab, gizmos, Combat Geometry prefab/physics tests | Projectile, Combat Debug, Damage/Hit Context, validator | Foundation invariant; provisional volumes follow Root pose | Prevents a required MainHull collider and localized-damage semantics | `Active` | Independent HP pools, localized damage, armor/penetration zones, modules, critical hits, or a fourth hull collider. | N/A | Scheme B Phase 3 RPD; Gelderland `CombatGeometry/MainHull/{Bow,Midship,Stern}` hierarchy; no-MainHull-collider, region-resolution, and root-transform tests. |
| `ART-LYR-001` | `CombatGeometry` layer / filter | Layer and query-filter convention | Combat Art / Combat Gameplay boundary | Dedicated Unity Layer named `CombatGeometry` at index `8` isolates query-only Combat hull colliders. The numeric index is part of the serialized project contract. | `ProjectSettings/TagManager.asset` layer entry, `LayerMask`, and region GameObject layer values | `Scalar / N/A`; mask bit is `1 << 8` | Physics queries in `World Space` | `CombatGeometry` layer index `8` in `TagManager.asset` plus authored region GameObject layers | Project architecture controls name/index; Combat Art authoring assigns region layers; runtime query consumers read/use the filter | Gelderland Combat prefab, Combat Geometry EditMode/PlayMode tests | Projectile, line-of-fire query, Combat Debug, validator | Static project/prefab configuration; index changes are breaking configuration changes | Filters Combat queries away from generic collision and unrelated environment geometry | `Active` | The Default layer, generic `ShipCollider` filtering, physical collision forces, or permission for Movement/Formation/Selection/Camera to depend on it. | N/A | `ProjectSettings/TagManager.asset` index `8`; Gelderland region layer values; layer identity, mask-isolation, and physics-query tests; `PRJ-CONV-005`. |
| `ART-CONV-009` | Combat Trigger query policy | Convention | Combat Art / Combat Gameplay boundary | Combat Geometry colliders are Triggers. Every Combat physics query must explicitly use the `CombatGeometry` LayerMask together with `QueryTriggerInteraction.Collide` or an equivalent explicit trigger-query policy. | Unity Trigger colliders and explicit physics-query arguments | `Scalar / N/A` | Physics queries in `World Space` | `PRJ-CONV-005`, authored Combat region Trigger flags, and the registered Combat Geometry layer | Project architecture and Combat Art authoring define policy/configuration; runtime Combat callers select the explicit query mode | Gelderland Combat prefab and Combat Geometry physics tests | Projectile, line-of-fire query, Combat Debug | Foundation invariant for every Combat geometry query | Makes queries independent of the global `Physics.queriesHitTriggers` setting | `Active` | Physical collision-force participation, implicit `QueryTriggerInteraction.UseGlobal`, or permission to add Rigidbody propulsion. | N/A | Gelderland Bow/Midship/Stern `isTrigger: true`; PlayMode queries pass `QueryTriggerInteraction.Collide` and verify `Ignore` misses; `PRJ-CONV-005`. |

## Exposure Reference

Phase 4A implements the code-only Target Exposure Reference. Phase 4B wires it
to the Gelderland Combat Root and adds passive Scene-view visualization using
the current Scene View camera position as editor-only observer input. It does
not add the planned `Exposure` child hierarchy, a debug-observer Transform, or
runtime observer state. The reference box is mathematical data and never
becomes a Collider.

| ID | Contract | Kind | Owner | Purpose / Meaning | Access / Type | Unit | Coordinate Space | Source of Truth | Writable By | Known / Planned Consumers | Lifecycle | Side Effects | Status | Do Not Interpret As | Replacement | Evidence |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `ART-PFB-005` | Gelderland Target Exposure integration | Prefab component contract | Combat Art | `PF_Ship_Gelderland_Combat_v01` has exactly one `ShipExposureReference` on the authoritative inherited Root, wired to the Root's existing `ShipArtDefinition`. No `Exposure` child, Collider, Rigidbody, or stored observer is introduced. | Authored component on the active Gelderland prefab variant | Dimensions `m`; area `m²`; reference `Scalar / N/A` | Art anchors use `Ship Root Local Space`; query results use `World Space` | Gelderland variant component assignment plus its existing `ShipArtDefinition` and `ArtReferences` | Combat Art prefab authoring changes assignment/calibration; Movement remains the only Root pose writer | Known: `ShipExposureReferenceGizmos`, prefab tests, PlayMode exposure integration tests. Planned: validator and Combat Gameplay consumers. | Static prefab configuration; on-demand queries follow Root movement/Heading | Adds a passive read component only; no physics, rendering, Movement, or Combat-state effects | `Active` | A second Root, runtime target assignment, `ExposureRect` GameObject, physics volume, final anchor calibration, or a generic-proxy component | N/A | Gelderland prefab `ShipExposureReference` assignment; `GelderlandExposurePrefabTests`; `GelderlandExposurePlayModeTests`. |
| `ART-API-011` | `ShipExposureReference` | `MonoBehaviour` component and read contract | Combat Art / spatial integration | Passive Target Exposure calculator bound to one `ShipArtDefinition`. It exposes reference dimensions/center and calculates one projected rectangle for an arbitrary observer position. | Addable component; getter-only `ArtDefinition`; three public `Try...` queries; no update loop | Dimensions `m`; area `m²` | Authored dimensions in `Ship Root Local Space`; result and observer in `World Space` | Serialized `ART-REF-019` plus the semantic anchors registered by `ShipArtDefinition` | Combat Art prefab/scene authoring assigns the art definition; runtime callers only request derived values | Known: Gelderland Combat variant, `ShipExposureReferenceGizmos`, and EditMode/PlayMode tests. Planned: Auto Target, Shot Sample/aim geometry, Combat Debug, validator. | Static serialized configuration with independent on-demand queries | Reads Transforms only; never writes pose, creates physics geometry, or mutates Combat/Movement state | `Active` | A target-lock component, shared current-observer state, Collider, dispersion calculator, hit resolver, or runtime debug-observer store | N/A | `ShipExposureReference.cs`; Gelderland prefab assignment; dimension, projection, failure, statelessness, dependency-boundary, and integration tests. |
| `ART-REF-019` | `ShipExposureReference.ArtDefinition` / `shipArtDefinition` | Serialized component reference and read API | Combat Art | Explicitly selects the `ShipArtDefinition` whose component Transform is the authoritative Ship Root and whose semantic anchors define the Exposure box. | Private `[SerializeField] ShipArtDefinition`; public getter-only `ShipArtDefinition ArtDefinition` | Reference `Scalar / N/A`; referenced positions `m` | Referenced anchors are interpreted in the art definition's `Ship Root Local Space` | Serialized `shipArtDefinition` field | Combat Art prefab/scene authoring; no public runtime setter | Known: Gelderland Combat variant, `ShipExposureReference`, gizmo, and prefab/integration tests. Planned: validator and Combat consumers through calculated results. | Static authored reference read on each query; missing reference fails safely | Reading has no side effects | `Active` | Permission to search the hierarchy, use a Renderer/Collider, store observer state, or write the referenced Root | N/A | `ShipExposureReference.ArtDefinition`; Gelderland serialized assignment; read-only/stateless and prefab-wiring tests. |
| `ART-REF-010` | Exposure Reference Box semantics | Mathematical spatial reference | Combat Art / spatial integration | Defines an oriented reference box with size `(Beam, Height, Length)` on Root-local `(X, Y, Z)`. No GameObject or BoxCollider is required. | Derived through `TryGetReferenceDimensions`; no stored box object | `m` | `Ship Root Local Space`, transformed to `World Space` only for query center/orientation | `ShipArtDefinition` Bow/Stern, Port/Starboard, Waterline/Deck semantic references | Derived read-only result; only Combat Art reference calibration changes the source anchors | `ShipExposureReference`; planned Auto Target, Shot Sample/aim geometry, Combat Debug, validator | Recomputed on demand so current authoritative Root pose is respected | None | `Active` | Combat Geometry bounds, generic `ShipCollider`, Render/Mesh bounds, a physics volume, screen-space size, or final formal-art calibration | N/A | `ShipExposureReference.TryGetReferenceDimensions`; source-of-truth and independence tests. |
| `ART-API-012` | `ShipExposureReference.TryGetReferenceDimensions(out Vector3 dimensionsMeters)` | Query API | Combat Art | Returns `(Beam, Height, Length)` when all required semantic dimensions are finite and greater than the minimum valid size; otherwise returns `false` and zero. | `public bool TryGetReferenceDimensions(out Vector3 dimensionsMeters)` | `m` | Vector axes map to `Ship Root Local Space` X/Y/Z semantics; returned magnitudes are orientation-independent | Length from `ART-API-002`; Beam from `ART-API-003`; Height from Waterline-to-Deck Root-local Y difference | Derived read-only result; nobody writes it | `ART-API-014`; planned validator/debug and Combat consumers | Stateless on-demand query | None | `Active` | World-axis bounds, angular/perspective size, or permission to cache mutable current Exposure | N/A | `ShipExposureReference.TryGetReferenceDimensions`; base-dimension, missing-reference, and zero-dimension tests. |
| `ART-API-013` | `ShipExposureReference.TryGetReferenceCenter(out Vector3 centerWorld)` | Query API | Combat Art | Returns a center whose Root-local X/Z come from `CenterReference` and whose Root-local Y is halfway between Waterline and Deck. Missing required anchors fail safely. | `public bool TryGetReferenceCenter(out Vector3 centerWorld)` | Position `m` | Derived in `Ship Root Local Space`; returned in `World Space` | `CenterReference` horizontal position plus the midpoint of `WaterlineReference` and `DeckReference` Root-local Y | Derived read-only result; nobody writes it | `ART-API-014`; planned validator/debug and Combat consumers | Stateless on-demand query following current Root pose | None | `Active` | `Renderer.bounds.center`, Combat Geometry center, center of mass, or CenterReference Y | N/A | `ShipExposureReference.TryGetReferenceCenter`; exact-center, translation, VisualRoot-independence, and missing-reference tests. |
| `ART-API-014` | `ShipExposureReference.TryCalculateExposure(Vector3, out ExposureRect)` | Query API | Combat Art | Calculates an independent Exposure rectangle for the supplied observer World position. It fails for missing/invalid references, invalid dimensions, non-finite input, a vertically degenerate Root forward, or effectively zero horizontal observer separation. | `public bool TryCalculateExposure(Vector3 observerWorldPosition, out ExposureRect exposure)` | Observer/center `m`; width/height `m`; area `m²` | Observer, result axes, and center in `World Space`; dimension semantics originate in `Ship Root Local Space` | Current `ART-REF-010` dimensions and `ART-API-013` center plus authoritative Root horizontal Heading | Derived read-only result; each caller owns its returned value | Known: EditMode tests. Planned: Auto Target, Shot Sample/aim geometry, Combat Debug. | Stateless per call; stores no current observer, shooter, targeter, aim rectangle, or current Exposure | No Transform, Movement, physics, weapon, or Combat-state writes | `Active` | Target acquisition, hit probability, distance falloff, accuracy, dispersion, projectile collision, or damage | N/A | `ShipExposureReference.TryCalculateExposure`; bearing, yaw, distance, statelessness, invalid-input, and no-write tests. |
| `ART-STA-001` | `ExposureRect` | Immutable query-result value | Combat Art / spatial integration | Carries the deterministic projected rectangle as `CenterWorld`, `PlaneNormalWorld`, `HorizontalAxisWorld`, `VerticalAxisWorld`, `WidthMeters`, and `HeightMeters`; `AreaSquareMeters` is their trivial product. | Public `readonly struct`; constructor plus getter-only properties | Positions/width/height `m`; area `m²`; axes unit vectors | All positions/axes in `World Space`; Vertical is World +Y | One successful `ART-API-014` calculation | Constructed only by the stateless query; consumers receive a value copy | Known: EditMode tests. Planned: Auto Target, Shot Sample/aim geometry, Combat Debug. | Immutable transient result per observer query | None | `Active` | A Collider, mutable shared state, screen rectangle, angular size, hit result, aim error, or damage context | N/A | `ExposureRect.cs`; axes, area, distance, and read-only contract tests. |
| `ART-CONV-002` | Target Exposure ownership boundary | Convention | Combat Art / Combat Gameplay boundary | `Exposure != CombatGeometry != Dispersion`. Exposure describes target projected geometry; Combat Geometry remains physical/query hit geometry; Dispersion remains future weapon/crew/technology error. None owns or mutates either of the others. | Foundation architecture boundary | Exposure `m`/`m²`; other systems retain their registered units | Exposure uses registered Root-local semantics and World-space query results | Exposure: `ShipArtDefinition` semantic references. Combat Geometry: authored region Colliders. Dispersion: future Combat-owned configuration | Architecture changes only; implementations remain within their own ownership boundary | Known: `ShipExposureReference`, `ShipCombatGeometry`, tests. Planned: Combat Gameplay and validator/debug. | Foundation invariant | Prevents source, state, or decision coupling among the three systems | `Active` | Exposure as hit geometry, Collider size as Exposure truth, Dispersion as target size, or Exposure as accuracy/Hit/Miss/Damage | N/A | Phase 4 RPD; Exposure source/dependency tests; existing Combat Geometry separation contracts. |
| `ART-CONV-010` | Exposure dimension and height convention | Geometric convention | Combat Art | Length is Bow-to-Stern Root-local Z; Beam is Port-to-Starboard Root-local X; Height is Waterline-to-Deck Root-local Y. Height intentionally represents gameplay hull height and excludes mast, sails, flags, rigging, lanterns, and decorative structures. | Applied by `ART-API-012` | `m` | `Ship Root Local Space` semantic axes | `ShipArtDefinition` semantic references only | Combat Art reference calibration; query results are read-only | `ShipExposureReference`; planned validator/debug and Combat consumers | Stable semantic rule; current anchor values remain provisional until formal-art calibration | None | `Active` | Renderer/Mesh/Collider bounds, Combat Geometry size, generic `ShipCollider`, or final placeholder dimensions | N/A | `ShipExposureReference`; dimension-source, renderer-free, collider-free, and missing-data tests. |
| `ART-CONV-011` | Horizontal observer and Exposure-plane axes | Coordinate-space convention | Combat Art | Observer bearing removes World Y. `PlaneNormalWorld` is normalized horizontal observer-to-center direction; `VerticalAxisWorld = Vector3.up`; `HorizontalAxisWorld = cross(World +Y, PlaneNormalWorld)`. Target pitch/roll and VisualRoot presentation tilt do not define Exposure. | Applied by `ART-API-014` | Unit directions `Scalar / N/A` | `World Space`; World +Y vertical | Observer World position, calculated center, and authoritative Root horizontal forward | Derived read-only result; nobody writes pose | `ExposureRect`; planned debug/aim consumers | Recomputed independently per query; zero horizontal separation fails | None | `Active` | Camera orientation, observer elevation, screen axes, VisualRoot roll, or permission to modify Heading | N/A | Horizontal-axis, coincident-observer, Root-yaw, and VisualRoot-independence tests. |
| `ART-CONV-012` | Continuous oriented-box projected width | Projection convention | Combat Art | `Width = abs(dot(V,F))*Beam + abs(dot(V,R))*Length`, where V is horizontal sight direction, F is authoritative Root horizontal forward, and R is its horizontal right. Bow/Stern-on therefore expose Beam; Port/Starboard broadside expose Length; intermediate bearings vary continuously. Projected Height equals Reference Height. | Applied by `ART-API-014` | Width/height `m` | World-space horizontal directions applied to Root-local semantic magnitudes | `ART-REF-010`, `ART-CONV-011`, and authoritative Root Heading | Derived read-only result; nobody writes it | `ExposureRect`; planned Auto Target, Shot Sample/aim geometry, Combat Debug | Stateless on-demand calculation | None | `Active` | Threshold angle categories, per-gun arcs, accuracy, hit probability, perspective projection, or target scoring | N/A | Bow, Stern, both broadside, intermediate-formula, continuity, yaw, and height-stability tests. |
| `ART-CONV-013` | Distance-independent world-meter Exposure | Unit/lifecycle convention | Combat Art / Combat Gameplay boundary | Observer position selects bearing only. Width, Height, and Area are geometric world-meter values and do not shrink with distance. Multiple observers query the same target independently without mutable shared current-observer/current-Exposure state. | Applied by `ART-API-014` and immutable `ART-STA-001` | Width/height `m`; area `m²` | `World Space` result geometry | Target semantic dimensions/orientation plus supplied bearing | Derived read-only result; each consumer owns its value copy | Planned Auto Target, Shot Sample/aim geometry, Combat Debug | One transient value per call; no cache or update loop | None | `Active` | Screen pixels, angular size, camera perspective, range quality, shared targeting state, or distance-based accuracy | N/A | Near/far equality, translation-invariance, sequential-observer, and read-only-state tests. |

## VFX integration hooks

Phase 5A implements event data and the receiver interface. Phase 5B implements
a scene-neutral placeholder receiver that creates short-lived primitives only
to verify the supplied World position and orientation. It does not provide an
event bus, gameplay emitter, final visual asset/system, automatic prefab
integration, or scene integration. Until future Combat resolution exists,
these values may only be constructed directly by tests or other explicitly
scoped callers.

| ID | Symbol / Contract | Kind | Owner | Purpose / Meaning | Access / Type | Unit | Coordinate Space | Source of Truth | Writable By | Known / Planned Consumers | Lifecycle | Side Effects | Status | Do Not Interpret As | Replacement | Evidence |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `ART-EVT-001` | Combat Art VFX event hook group | Event boundary | Combat Gameplay emits; Combat Art/VFX consumes | Defines one-way notification of gameplay-decided muzzle fire, water impact, and hull impact facts to presentation receivers through `ART-API-015`. | `ART-EVT-002` through `ART-EVT-004` value data passed to `ICombatVFXEventReceiver` methods | Per event contract | Per event contract | The applicable gameplay outcome and its registered spatial source; Phase 5 supplies no outcome producer | Future Combat Gameplay constructs event values; VFX reads the supplied values and owns only presentation | Known: contract tests and `CombatVFXPlaceholderReceiver`. Planned: Combat Gameplay emitters and final Combat Art/VFX receivers. | Runtime transient after the authoritative gameplay decision; no storage or dispatch lifecycle is established | Construction has no side effects; the placeholder receiver may create presentation only | `Active` | An event bus, global manager, gameplay outcome decision, Projectile implementation, final VFX system, or Movement command channel | N/A | Event/interface source; `CombatVFXEventContractTests`; `CombatVFXPlaceholderReceiver.cs`; `CombatVFXPlaceholderReceiverPlayModeTests`. |
| `ART-EVT-002` | `CombatMuzzleFireEvent` | Immutable event data | Combat Gameplay creates; Combat Art/VFX consumes | Reports that gameplay already decided one cannon fired, preserving its muzzle World position/direction, source-ship identity, and registered muzzle-socket identifier. | Public `readonly struct`; constructor; getter-only `PositionWorld`, `DirectionWorld`, `SourceShip`, and `MuzzleIdentifier` | Position `m`; direction and identity `Scalar / N/A` | Position/direction: `World Space`; ship and muzzle identity: `Scalar / N/A` | Fire decision: future Combat Gameplay. Position/direction and identifier: the selected Transform in `ShipMuzzleSockets.PortMuzzles` or `.StarboardMuzzles`; source identity: authoritative ship Root `GameObject` | Future Combat Gameplay copies values during construction; immutable afterward | Known: EditMode contract tests and `CombatVFXPlaceholderReceiver`. Planned: final receiver implementations. | One transient value per gameplay-decided muzzle fire | Construction and reading have no side effects | `Active` | Reload state, accuracy, damage, Projectile settings, permission to fire, a mutable socket reference, or ship Transform ownership | N/A | `CombatMuzzleFireEvent.cs`; preservation, immutability, dependency, independence, and placeholder spatial tests. |
| `ART-EVT-003` | `CombatWaterImpactEvent` | Immutable event data | Combat Gameplay creates; Combat Art/VFX consumes | Reports that gameplay already decided a projectile missed and that a water-impact visual is required, preserving the resolved World position and surface normal. | Public `readonly struct`; constructor; getter-only `PositionWorld` and `NormalWorld` | Position `m`; normal `Scalar / N/A` | `World Space` | Future authoritative Combat/Projectile resolution; Phase 5A does not calculate or emit the result | Future Combat Gameplay copies values during construction; immutable afterward | Known: EditMode contract tests and `CombatVFXPlaceholderReceiver`. Planned: final receiver implementations. | One transient value per gameplay-decided water impact | Construction and reading have no side effects | `Active` | Miss detection, Projectile simulation, final splash effects, damage, perspective data, or physics-query ownership | N/A | `CombatWaterImpactEvent.cs`; preservation, immutability, dependency, independence, and placeholder spatial tests. |
| `ART-EVT-004` | `CombatHullImpactEvent` | Immutable event data | Combat Gameplay creates; Combat Art/VFX consumes | Reports that gameplay already decided a projectile hit a ship surface, preserving its resolved World position/normal and target-ship identity. Phase 5A does not include optional Combat Geometry region data. | Public `readonly struct`; constructor; getter-only `PositionWorld`, `NormalWorld`, and `TargetShip` | Position `m`; normal and identity `Scalar / N/A` | Position/normal: `World Space`; target identity: `Scalar / N/A` | Future authoritative Combat/Projectile resolution and its resolved target ship; Phase 5A does not calculate or emit the result | Future Combat Gameplay copies values during construction; immutable afterward | Known: EditMode contract tests and `CombatVFXPlaceholderReceiver`. Planned: final receiver implementations. | One transient value per gameplay-decided hull impact | Construction and reading have no side effects | `Active` | Hit detection, damage, armor, Integrity, localized damage, Combat Geometry ownership, or permission to mutate the target | N/A | `CombatHullImpactEvent.cs`; preservation, immutability, dependency, independence, and placeholder hull-surface tests. |
| `ART-API-015` | `ICombatVFXEventReceiver` | Receiver interface | Combat Art/VFX | Minimal presentation receiver boundary with one method for each Active VFX event value. Implementers consume facts already decided by gameplay. | Public interface: `OnMuzzleFire(CombatMuzzleFireEvent)`, `OnWaterImpact(CombatWaterImpactEvent)`, and `OnHullImpact(CombatHullImpactEvent)` | Per event contract | Per event contract | The immutable event argument supplied by a future gameplay-owned emitter | Future Combat Gameplay invokes; VFX implementations may change presentation only and must not mutate gameplay outcomes or Movement | Known: EditMode contract tests and `CombatVFXPlaceholderReceiver`. Planned: final visual-effect receivers and future Combat emitters. | Invocation timing follows a future authoritative gameplay decision; no registration, dispatch ordering, or global receiver lifecycle is established | Implementations may cause presentation-only effects | `Active` | A VFX manager, singleton, service locator, event bus, gameplay callback, outcome resolver, or permission to modify event/source/target state | N/A | `ICombatVFXEventReceiver.cs`; exact-method-surface contract tests; `CombatVFXPlaceholderReceiver.cs`. |
| `ART-API-016` | `CombatVFXPlaceholderReceiver` | Runtime placeholder receiver | Combat Art/VFX | Maps each immutable Phase 5 event to one short-lived primitive at the exact supplied World pose: muzzle local forward follows DirectionWorld; water/hull local up follows NormalWorld. | Public sealed `MonoBehaviour`; implements `ART-API-015`; three event receiver methods | Position/scale `m`; direction/normal `Scalar / N/A` | Spawn pose: `World Space`; placeholder scale: object local | Immutable event values supplied by the caller | VFX receiver owns only its enable flag and objects it creates; it does not create gameplay events or write source/target/Movement state | Known: PlayMode spatial verification. Planned: replacement by final VFX presentation | On invocation when enabled; spawned primitives self-destroy after a temporary two-second lifetime; no retained per-event state | Instantiates presentation primitives; disables and schedules removal of their generated Colliders | `Active` | A gameplay emitter, VFX manager, pool, Projectile, outcome resolver, final VFX system, prefab requirement, or permission to modify Movement | Final VFX implementation, when separately designed | `CombatVFXPlaceholderReceiver.cs`; `CombatVFXPlaceholderReceiverPlayModeTests`. |
| `ART-FLD-001` | `CombatVFXPlaceholderReceiver.VisualSpawningEnabled` / `visualSpawningEnabled` | Configuration field/property | Combat Art/VFX | Enables or suppresses placeholder-object creation without changing event values, ship state, Combat Art references, or Movement. | Private serialized `bool`; public get/set property; default `true` | `Scalar / N/A` | `Scalar / N/A` | Receiver instance configuration | Inspector/runtime presentation configuration only | Placeholder spatial test workflow | Read on every receiver invocation | When `false`, receiver methods have no side effects | `Active` | A gameplay pause, Combat enable flag, event-consumption state, fire permission, or Movement control | Final VFX enable/configuration contract, if designed | `CombatVFXPlaceholderReceiver.cs`; disabled-receiver PlayMode test. |
| `ART-CONV-014` | Placeholder VFX pose and isolation | Spatial/presentation convention | Combat Art/VFX | Placeholder visuals copy event World position exactly. Muzzle local forward aligns to supplied direction; impact local up aligns to supplied normal. Spawned objects are unparented, non-authoritative, and collider-disabled. Shape, scale, color, and lifetime are temporary diagnostics rather than final art data. | Applied by `ART-API-016` | Position/scale `m`; direction/normal `Scalar / N/A` | `World Space` event pose | `ART-EVT-002` through `ART-EVT-004` | VFX writes only created placeholder objects; event/source/target/Ship Root Transform data remain read-only | PlayMode spatial tests; manual placeholder validation | Per event invocation; no gameplay dependency | Presentation-only object creation when enabled | `Active` | Final visual tuning, collision geometry, authoritative impact data, a gameplay effect, or Transform ownership | Final VFX implementation, when separately designed | `CombatVFXPlaceholderReceiver.cs`; position/orientation, moving/rotating ship, hull-surface, disabled, and ownership tests. |
| `ART-CONV-003` | VFX authority boundary | Convention | Combat Art / Combat Gameplay boundary | Gameplay owns whether muzzle fire, miss/water impact, or hit/hull impact happened and owns the supplied positions, directions, and normals. VFX owns visual spawning, lifetime, and presentation only. VFX must not decide Hit/Miss/Damage/Projectile results, change Integrity, or modify Movement. | Foundation architecture boundary applied by `ART-EVT-001` through `ART-EVT-004` and `ART-API-015` | `Scalar / N/A`; spatial payloads retain their registered units | Spatial payloads retain their registered spaces | Authoritative future Combat outcome plus the immutable event value delivered to VFX | Architecture changes only; Gameplay creates facts, VFX consumes them read-only | Combat VFX contracts, placeholder receiver, and tests; planned Combat Gameplay/final VFX implementations | Foundation invariant; applies to every receiver invocation | Prevents presentation code from acquiring gameplay or Movement authority | `Active` | Gameplay authority, bidirectional mutation, damage state, Projectile ownership, Root pose ownership, or proof of a final visual system | N/A | Phase 0.5 architecture; Phase 5A event/interface source; Phase 5B receiver; dependency-boundary and ownership tests. |

## Combat Art Validator

| ID | Contract | Kind | Owner | Purpose / Meaning | Access / Type | Unit / Space | Source of Truth | Writable By | Known / Planned Consumers | Lifecycle | Side Effects | Status | Do Not Interpret As | Replacement | Evidence |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `ART-TOOL-001` | Combat Art Validator | Editor tool | Combat Art tooling | Planned Editor-only configuration checker for hierarchy, required references, socket organization, spaces, and geometry separation. Detailed checks await contract implementation. | Editor-only validation command/tool; API TBD | `Scalar / N/A` | Implemented Registry contracts plus inspected asset/prefab configuration | Tooling code may report diagnostics; it must not silently author/repair prefabs | Artists, designers, developers, CI only if explicitly added later | `Editor-only` and on demand | Reports diagnostics only when implemented | `Planned` | An automatic prefab builder, runtime dependency, or substitute for manual visual/spatial validation. | N/A | Approved Phase 0.5 / Change 3 architecture; implementation evidence: none. |
| `ART-TOOL-002` | Muzzle socket Scene-view gizmos | Editor visualization | Combat Art tooling | Passively visualizes each collected muzzle position, nominal forward direction, side, and canonical sequence label for a selected or active `ShipMuzzleSockets` component. Port is red and Starboard is blue. | Editor-only `[DrawGizmo]`; spheres, direction lines, and `P01` / `S01`-style labels | Positions `m`; direction `Scalar / N/A`; `World Space` visualization derived from authored Transforms | Active `ShipMuzzleSockets` read collections and each socket Transform | Tool code draws only; nobody is authorized to mutate prefab or Movement state through this contract | Known: artists/developers inspecting muzzle configuration. Planned: validator workflow. | `Editor-only`, Scene view, selected/active draw pass | Draws transient Scene-view geometry and labels only; no hierarchy or state writes | `Active` | Runtime VFX, a Combat decision, validation pass/fail, automatic prefab authoring, or final socket calibration. | N/A | `ShipMuzzleSocketGizmos.cs`; compile coverage; manual Scene-view verification remains required. |
| `ART-TOOL-003` | Combat Geometry Scene-view gizmos | Editor visualization | Combat Art tooling | Passively displays Bow, Midship, and Stern query volumes with distinct wireframe colors and semantic labels, making the gameplay hit hull visible independently of Render Mesh and generic collision. | Editor-only `[DrawGizmo]` over `ShipCombatGeometry`; BoxCollider-local wireframes with bounds fallback | Positions/sizes `m`; `World Space` visualization derived from authored collider transforms | Active `ShipCombatGeometry` region references and each `CombatHitRegion.QueryCollider` | Tool code draws only; nobody is authorized to mutate prefab, collider configuration, or Movement state through this contract | Known: artists/developers inspecting Gelderland Combat Geometry. Planned: validator workflow and Combat Debug. | `Editor-only`, Scene view, selected/active draw pass | Draws transient Scene-view wireframes and labels only; no hierarchy, collider, or state writes | `Active` | Runtime Combat UI, physics query execution, validation pass/fail, automatic hull fitting, mesh analysis, or final geometry calibration. | N/A | `ShipCombatGeometryGizmos.cs`; source boundary inspection; manual Scene-view verification remains required. |
| `ART-TOOL-004` | Target Exposure Scene-view gizmo and observer input | Editor visualization | Combat Art tooling | Draws the calculated `ExposureRect`, center, horizontal/vertical axes, plane normal, observer-to-target line, and Width/Height/Area label for a selected or active `ShipExposureReference`. The current Scene View camera position is the observer supplied to the stateless runtime query. | Editor-only `[DrawGizmo]` over `ShipExposureReference`; `SceneView.currentDrawingSceneView.camera.transform.position` is transient tool input | Position/width/height `m`; area `m²`; axes `Scalar / N/A` | Scene View observer and all drawn geometry use `World Space` | Active `ShipExposureReference.TryCalculateExposure` output plus transient Scene View camera position | Tool code draws only; moving the Scene View camera is an editor interaction and writes no ship/prefab/gameplay state | Known: artists/developers inspecting Gelderland Target Exposure. Planned: validator workflow and separate Combat Debug if designed later. | `Editor-only`, Scene view, selected/active draw pass; recalculated on each gizmo draw | Draws transient handles/labels only; no observer Transform, hierarchy, Collider, target assignment, or runtime state is created | `Active` | Current shooter/targeter, Combat Source of Truth, runtime target indicator, HUD, target acquisition, weapon owner, or saved prefab data | N/A | `ShipExposureReferenceGizmos.cs`; editor assembly compile; manual Scene-view readability/movement verification remains required. |

## Formal art dependency

| ID | Contract | Kind | Owner | Purpose / Meaning | Source of Truth | Writable By | Planned Consumers | Lifecycle | Status | Do Not Interpret As | Evidence |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `ART-CONV-004` | Placeholder-capable Phases 1–6 | Convention | Combat Art / project architecture | Phases 1–6 must remain placeholder-capable. Current 45 m Length, 11 m Beam, 2 m gameplay hull Exposure Height, waterline Y, center Y, and deck Y are temporary Medium-proxy calibration; final transforms, dimensions, sockets, and geometry are recalibrated when formal Gelderland art arrives. | This Registry contract, `PF_Ship_Gelderland_Combat_v01`, and later validated formal Gelderland asset data | Architecture and Combat Art calibration change only | Known: Combat placeholder prefab, Target Exposure integration, and tests. Planned: all later Combat Art and Combat Gameplay phases. | Foundation through formal-art integration | `Active` | Placeholder transforms/dimensions/sockets/geometry as final production art data or frozen historical truth. | Combat placeholder variant and tests; formal Gelderland calibration evidence: not yet available. |

The Phase 1 code and placeholder-prefab contracts are implemented by
`ART-API-001` through `ART-API-003`, `ART-PFB-004`, `ART-REF-001` through
`ART-REF-009`, and `ART-CONV-004` through `ART-CONV-005`. Phase 2A adds the
Active code-only contracts `ART-API-004` through `ART-API-006`, `ART-REF-011`
through `ART-REF-012`, and `ART-CONV-006` through `ART-CONV-007`. Phase 2B
activates the Gelderland implementation contracts `ART-PFB-002`,
`ART-SCK-001`, `ART-SCK-002`, and `ART-TOOL-002`, and expands the evidence for
`ART-PFB-004`. Active placeholder entries do not assert final art data.
Phase 3A activates the code-only Combat Geometry contracts `ART-API-007`
through `ART-API-010`, `ART-REF-013` through `ART-REF-018`,
`ART-CONV-001`, and `ART-CONV-008`. Phase 3B activates the Gelderland
`ART-PFB-003` hierarchy, `ART-LYR-001` layer/filter, `ART-CONV-009` Trigger
query policy, and `ART-TOOL-003` Scene-view visualization, and adds prefab and
physics evidence to the Phase 3A reference contracts. Phase 4A activates the
code-only `ART-API-011` through `ART-API-014`, `ART-STA-001`, `ART-REF-010`,
`ART-REF-019`, `ART-CONV-002`, and `ART-CONV-010` through `ART-CONV-013`.
Phase 4B activates `ART-PFB-005` and `ART-TOOL-004`, integrates the existing
Phase 4A component on `ART-PFB-004`, and adds prefab/PlayMode evidence to the
Phase 4A contracts. Phase 5A activates the code-only `ART-EVT-001` through
`ART-EVT-004`, `ART-API-015`, and `ART-CONV-003` VFX notification boundary.
Phase 5B activates the placeholder-only `ART-API-016`, `ART-FLD-001`, and
`ART-CONV-014` spatial presentation contracts. The optional `Exposure` child
hierarchy, runtime Combat Debug, final VFX assets and spawning system, the
validator, and formal-art calibration remain Planned; future tasks must promote
only implemented contracts and add source/prefab/test evidence in the same
logical change.
